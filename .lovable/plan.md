وجدت سبباً واضحاً الآن: زر نافذة القواعد موجود كـ Dialog فقط، لكن لا يوجد زر فعلي يفتح `setShowRulesDialog(true)` في الجزء الظاهر من `TranslationAIEnhancePanel.tsx`. لذلك طبيعي ألا يظهر عندك أي زر. كذلك مسار `/index` غير موجود في الراوتر، بينما المحرر الحقيقي على `/editor`؛ إذا كنت على `/index` فلن ترى تعديلات المحرر أصلاً.

الخطة عند الموافقة:

1. إصلاح زر القواعد في أداة تحسين الترجمة
   - الملف: `src/components/editor/TranslationAIEnhancePanel.tsx`
   - أضيف زر واضح دائماً داخل رأس البطاقة، بجانب زر الإعدادات، يكتب: `القواعد` ويفتح `EnhanceRulesDialog`.
   - أجعله مناسباً للموبايل 393px بدون أن يختفي بسبب `truncate` أو flex.
   - التحقق: وجود `onClick={() => setShowRulesDialog(true)}` فعلياً في زر مرئي.

2. إزالة أي التباس في DeepSeek داخل مقارنة المحركات
   - الملف: `src/components/editor/CompareEnginesDialog.tsx`
   - أثبت قائمة المقارنة لتعرض DeepSeek V4 Flash و DeepSeek V4 Pro فقط.
   - لا أضيف `deepseek-chat` أو `deepseek-reasoner` للواجهة؛ تبقى فقط كأسماء API داخل الدوال الخلفية عند الإرسال الرسمي، وهذا طبيعي لأن DeepSeek API لا يقبل إلا هذه الأسماء داخلياً.
   - التحقق: قائمة المقارنة تحتوي `deepseek-v4-flash` و `deepseek-v4-pro` ولا تحتوي المحركات القديمة في الواجهة.

3. توضيح المسار الصحيح للمحرر
   - إذا لزم، أراجع سبب أنك على `/index`، لأن التطبيق لا يحتوي Route باسم `/index`.
   - الهدف: بعد التعديل تكون رؤية الزر في صفحة `/editor` داخل بطاقة `تحسين الترجمة بالذكاء الاصطناعي`.

لن أغيّر أي شيء آخر خارج هذه الملفات إلا إذا ظهر أن `/index` هو سبب عدم رؤيتك للتحديثات.