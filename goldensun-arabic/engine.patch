diff --git a/asm/ui/ui/AdvanceMsgText.s b/asm/ui/ui/AdvanceMsgText.s
index d3489a5..f103170 100644
--- a/asm/ui/ui/AdvanceMsgText.s
+++ b/asm/ui/ui/AdvanceMsgText.s
@@ -639,7 +639,7 @@
 	lsl	r3, #16
 	lsl	r1, #12
 	cmp	r3, r1
-	bhi	.L16e3e
+	b	.L16e3e
 	lsl	r3, r4, #8
 	orr	r7, r3
 	ldrh	r3, [r6, #0x12]
diff --git a/asm/ui/ui/BufferString.s b/asm/ui/ui/BufferString.s
index c94ce1d..e50fdd5 100644
--- a/asm/ui/ui/BufferString.s
+++ b/asm/ui/ui/BufferString.s
@@ -1,7 +1,7 @@
 	.include "macros.inc"
 	.include "gba.inc"
 
-.thumb_func_start BufferString  @ 0x08018038
+.thumb_func_start BufferString_orig  @ 0x08018038
 	push	{r5, r6, r7, lr}
 	mov	r7, r11
 	mov	r6, r10
@@ -838,5 +838,5 @@
 	pop	{r5, r6, r7}
 	pop	{r1}
 	bx	r1
-.func_end BufferString
+.func_end BufferString_orig
 
diff --git a/asm/ui/ui/DrawText.s b/asm/ui/ui/DrawText.s
index 882c099..b38f142 100644
--- a/asm/ui/ui/DrawText.s
+++ b/asm/ui/ui/DrawText.s
@@ -1,7 +1,7 @@
 	.include "macros.inc"
 	.include "gba.inc"
 
-.thumb_func_start DrawText  @ 0x08018cac
+.thumb_func_start DrawText_orig  @ 0x08018cac
 	push	{r5, r6, r7, lr}
 	mov	r7, r11
 	mov	r6, r10
@@ -277,5 +277,5 @@
 	pop	{r5, r6, r7}
 	pop	{r1}
 	bx	r1
-.func_end DrawText
+.func_end DrawText_orig
 
diff --git a/asm/ui/ui/Func_8017aa4.s b/asm/ui/ui/Func_8017aa4.s
index f8ed4c5..d5f6389 100644
--- a/asm/ui/ui/Func_8017aa4.s
+++ b/asm/ui/ui/Func_8017aa4.s
@@ -135,7 +135,7 @@
 	lsl	r3, #16
 	lsl	r1, #12
 	cmp	r3, r1
-	bhi	.L17bd4
+	b	.L17bd4
 	lsl	r3, r0, #8
 	orr	r4, r3
 	add	r5, #2
diff --git a/data/rom_15000/rom_32224.s b/data/rom_15000/rom_32224.s
index f48b585..8c85033 100644
--- a/data/rom_15000/rom_32224.s
+++ b/data/rom_15000/rom_32224.s
@@ -2,4 +2,6 @@
 
 	.section .rodata
 
-	.incdata Data_32224, 0x32224, 0x33e40
+	.global Data_32224
+Data_32224:
+	.incbin "data/arabic_font.bin"
diff --git a/src/decompress/huffman.s b/src/decompress/huffman.s
index f0bf98c..91b819f 100644
--- a/src/decompress/huffman.s
+++ b/src/decompress/huffman.s
@@ -129,7 +129,7 @@
 	str	r0, [sp, #0x28]
 	sub	r12, r1, #0x20
 	ldrh	r0, [r0, #0xa]
-	cmp	r12, #0x6f
+	cmp	r12, #0xdf
 	str	r0, [sp, #0x1c]
 	movhi	r0, #1
 	bhi	.L158dc
diff --git a/src/ui/ui.c b/src/ui/ui.c
index 1192c61..a94c0f0 100644
--- a/src/ui/ui.c
+++ b/src/ui/ui.c
@@ -340,3 +340,103 @@ INCLUDE_ASM("asm/ui/ui/Func_8019d0c.s");
 INCLUDE_ASM("asm/ui/ui/GetPortrait.s");
 INCLUDE_ASM("asm/ui/ui/Func_8019da8.s");
 INCLUDE_ASM("asm/ui/ui/Func_8019e48.s");
+
+/* ---- Arabic: right-to-left text ------------------------------------------
+ * Text is stored in logical order. DrawText mirrors each glyph's x inside its
+ * window, so the first letter lands on the right, every line is right-aligned
+ * and the typewriter starts from the right. A run of Latin letters or digits
+ * (an English name, a number the game fills in) must still read left to
+ * right: BufferString flips each such run once the text is expanded, and the
+ * mirroring flips it back. */
+
+extern unsigned char *gRtlTextBase __asm__("iwram_3001e8c");
+extern const unsigned short Data_32224[];
+extern int BufferString_orig(unsigned int id, int flag);
+extern int DrawText_orig(void *win, unsigned int ch, int x, int y, int flag);
+
+/* Extra halfwords each control code carries, as Func_8017aa4 skips them.
+   (A function, not a table: this unit's .rodata is not linked.) */
+static int RtlCodeArgs(unsigned int c)
+{
+    if ((c >= 0x08 && c <= 0x0c) || c == 0x11 || c == 0x1d)
+        return 1;
+    if (c == 0x0e || c == 0x0f || c == 0x1c)
+        return 2;
+    return 0;
+}
+
+static int RtlIsLtr(unsigned int c)
+{
+    return (c >= '0' && c <= '9') || (c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z');
+}
+
+/* Arabic letters: 0x8C-0xFF, and the ASCII codes lent to the rarest forms. */
+static int RtlIsArabic(unsigned int c)
+{
+    if (c >= 0x8c && c <= 0xff)
+        return 1;
+    return c == '<' || c == '>' || c == '@' || (c >= '[' && c <= '^') || c == '`' || (c >= '{' && c <= 0x7f);
+}
+
+int BufferString(unsigned int id, int flag)
+{
+    unsigned short *buf = (unsigned short *)(gRtlTextBase + 0xeb0);
+    int start = BufferString_orig(id, flag);
+    int i = start;
+    int n;
+
+    /* id -1 hands back the string already in the buffer, flipped already */
+    if (id == 0xffffffff)
+        return start;
+
+    /* up to the string's NUL: when only measuring (flag 0) the write index
+       does not move, so it cannot mark the end */
+    for (n = 0; n < 0x200 && buf[i] != 0; n++) {
+        unsigned int c = buf[i];
+        int j, last;
+
+        if (c < 0x20) {
+            i = (i + 1 + RtlCodeArgs(c)) & 0x1ff;
+            continue;
+        }
+        if (!RtlIsLtr(c)) {
+            i = (i + 1) & 0x1ff;
+            continue;
+        }
+        /* the run: from here to the last Latin letter or digit before the next
+           Arabic letter or control code, neutral marks between them included */
+        last = i;
+        for (j = i; buf[j] != 0; j = (j + 1) & 0x1ff) {
+            c = buf[j];
+            if (c < 0x20 || c > 0xff || RtlIsArabic(c))
+                break;
+            if (RtlIsLtr(c))
+                last = j;
+        }
+        j = last;
+        while (i != j) {
+            unsigned short t = buf[i];
+            buf[i] = buf[j];
+            buf[j] = t;
+            i = (i + 1) & 0x1ff;
+            if (i == j)
+                break;
+            j = (j - 1) & 0x1ff;
+        }
+        i = (last + 1) & 0x1ff;
+    }
+    return start;
+}
+
+int DrawText(void *win, unsigned int ch, int x, int y, int flag)
+{
+    if (ch > 0x20 && ch <= 0xff) {
+        int w = Data_32224[(ch - 0x20) * 16];
+        int bound = *(unsigned short *)((unsigned char *)win + 8) * 8 - 12;
+
+        x = bound - x - w;
+        if (x < 0)
+            x = 0;
+    }
+    return DrawText_orig(win, ch, x, y, flag);
+}
diff --git a/tools/pack_strings.c b/tools/pack_strings.c
index 1b151fa..f9192e1 100644
--- a/tools/pack_strings.c
+++ b/tools/pack_strings.c
@@ -82,7 +82,7 @@ static void read_strings(FILE *fp) {
 	char chr = '\0';
 
 	while ((c = fgetc(fp)) != EOF) {
-		CHECK(c >= 0 && c <= CHAR_MAX);
+		CHECK(c >= 0 && c <= 0xff);
 		chr = c;
 
 		if (nchars == text_len) {
