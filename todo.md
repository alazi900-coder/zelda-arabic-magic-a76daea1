# مهام تعريب Yu-Gi-Oh! Reshef

## فحص خط Kingdom Hearts: Birth by Sleep

## فحص حزمة LumenTale: Memories of Trey

- [x] فحص حزمة `localization-string-tables-english_assets_all.bundle` للقراءة فقط: UnityFS 2022.3.62f2، 30 جدول نص إنجليزي، و14,476 سجلاً. لم يُعدّل الملف أو يُعاد بناؤه.
- [x] إضافة قسم LumenTale إلى محرر الأداة لاستيراد جداول Unity الإنجليزية وعرض السجل بواسطة اسم الجدول و`m_Id`.
- [x] حماية `m_Id` والرموز التقنية وعلامات التنسيق من أي تعديل أثناء الترجمة أو الاستيراد؛ حارس منفصل يرفض الحذف أو الترجمة أو تبديل ترتيب الرموز.
- [x] إضافة برومبتات وقاموس مصطلحات للترجمة التلقائية يخصان عالم LumenTale دون التأثير على إعدادات الألعاب الأخرى.
- [x] اختبار القراءة على الحزمة الأصلية فقط: 30 جدولاً، و14,476 سجلاً، و13,908 نصوص غير فارغة، مع الحفاظ على خريطة المورد واسم الجدول و`m_Id` النصي.
- [x] اختبار حارس الرموز وبناء الإنتاج: 8/8 اختبارات LumenTale نجحت، وبناء Vite نجح.
- [x] إنشاء خريطة بناء LumenTale تحفظ `assetPath` و`pathId` واسم الجدول و`m_Id` لكل سجل قابل للكتابة.
- [x] اختبار round-trip للحزمة على نسخة عمل منفصلة: قراءة ثم كتابة ثم إعادة فتح مع مقارنة 30 جدولاً والموارد والهوية.
- [x] اختبار تعديل سطر واحد على نسخة عمل والتحقق من بقاء بقية الجداول والموارد سليمة قبل تفعيل التصدير.
- [x] ربط زر بناء Bundle LumenTale داخل المحرر بعد نجاح اختبارات السلامة فقط؛ يبني زر التنزيل نسخة جديدة ولا يغيّر المصدر المحلي.
- [x] إنشاء commit ودفع تحديث LumenTale إلى GitHub بعد نجاح اختبارات البناء وإعادة الفتح؛ لا يُضمّن الملف الأصلي أو نسخ الاختبار في المستودع. (`6974f1c`)
- [x] فحص أرشيف LumenTale المرفوع قراءةً فقط: يحتوي حزم خطوط UnityFS فقط؛ فُتحت 20 حزمة، وتعذر فتح `dongle` و`saira` بسبب نهاية بيانات غير متوقعة. لم تُعدّل أو تُستبدل أي ملفات عمل.
- [x] فحص خطوط LumenTale نفسها: استُخرجت جداول محارف 34 أصل TMP/SDF من 20 حزمة مقروءة؛ لا توجد أي محارف عربية ضمن U+0600–U+06FF أو U+0750–U+077F أو U+FB50–U+FDFF أو U+FE70–U+FEFF. تعذر فحص `dongle` و`saira` لأن النسخ المستخرجة أقصر من الحجم المعلن في الأرشيف؛ لم تُعدّل أي ملفات.
- [x] تحديد أصول خط TMP التي تستدعيها LumenTale ومسار الـFallback قبل إدخال العربية: Barlow SemiCondensed Regular وMedium يشيران بالفعل إلى `DroidSansFallback SDF` داخل حزمة Barlow نفسها.
- [x] تقسيم نصوص LumenTale في المحرر إلى بطاقات وفلاتر أقسام مستقلة: القوائم، المهارات، القتال، المهام، الحوارات، الشخصيات، العناصر، السجل/العالم، النظام، وأخرى للنصوص التي لا تمنح بيانات الجدول أي سياق عنها. الفلتر وبطاقات التقدم والترجمة الآلية تستخدم المصنّف نفسه. نجحت اختبارات التصنيف وحماية الرموز والفحص العميق، ونجح بناء Vite.
- [x] فحص أداة تحسين الترجمة بالذكاء الاصطناعي في LumenTale: الواجهة تمرر `game: lumentale` فعلاً، لكن خادم التحسين لا يملك فرعاً خاصاً لـLumenTale ويستخدم حارساً عاماً لا يطابق `lumentale-token-guard` في وسوم Unity/TMP ومتغيرات الخصائص وترتيب الرموز. لم يُطبّق إصلاح لهذه الفجوة في هذه المهمة.
- [x] ربط أداة تحسين الترجمة بالذكاء الاصطناعي بحارس LumenTale الصارم في الواجهة والخادم: الخادم والواجهة يرفضان الآن حذف أو تغيير قيمة أو تبديل ترتيب رموز Unity/TMP وplaceholders وescapes ووسوم التحكم وprintf، وتُفلتر الاقتراحات والبدائل قبل العرض والتخزين والتطبيق. نجحت اختبارات الحارس المرآتي 10/10 وبناء Vite. بقي `npm test -- --run` العام غير أخضر بسبب 11 فشلاً في 10 ملفات خارج LumenTale (ومنها اختبارات MOTHER 3)؛ لم تُعدّل تلك المسارات هنا.
- [x] التحقق من سجل تعديلات خطوط LumenTale: سجل تدقيق حزمة Barlow العربية يثبت أن الكائنين المعدلين فقط كانا `DroidSansFallback SDF` وصفحة Atlas 3 التابعة له؛ بقيت 78/80 كائناً مطابقة للأصل، ومنها أصول Barlow الأساسية. لا يوجد في سجلات Git أو ملفات المشروع المتاحة دليل على تعديل لاحق لأصول خطوط إضافية.
- [ ] تشخيص صغر العربية والتباعد البسيط بين حروفها في مورد `DroidSansFallback SDF`، ثم إعداد تعديل محدود في glyph metrics أو الرسم العربي فقط، من دون تعديل الإنجليزية أو أصول الخط الأخرى أو بناء حزمة جديدة قبل موافقة المستخدم.
- [ ] تشخيص الفراغات الظاهرة بين الحروف العربية في LumenTale من ملفات TMP/SDF ومواد الواجهة الفعلية: إثبات هل السبب advance/kerning أو character spacing أو Font Asset/Material بديل، ثم اقتراح أقل إصلاح ممكن. ممنوع تعديل حزمة الخط أو النص أو الموارد الإنجليزية قبل التحقق.
- [ ] فحص `catalog.json` في LumenTale وعلاقته بحزم Addressables المعدلة: تحديد حزم الخط والنص المشار إليها وCRC/التواقيع المرتبطة بها، ثم التحقق من صحة مسار `patchcrc` في AddressablesTools على نسخة عمل فقط قبل أي تبديل داخل اللعبة. لا يُعدل الكتالوج أو الخط أو النص الأصليان خلال الفحص.
- [ ] فحص حزمة `team17_fonts_assets_barlow-semicondensed(2).bundle` الجديدة مقابل مورد الخط المرجعي لتفسير فجوة الوصل الظاهرة في لقطة اللعبة: استخراج TMP Font Asset وglyph metrics والـAtlas ومادة العرض، ثم تحديد هل مصدرها advance أو bearing أو padding أو عرض بديل. لا يُعدل أي ملف في هذه المرحلة.
- [ ] قياس حدود الحبر الفعلية لأشكال العربية المتصلة داخل Atlas حزمة Barlow الجديدة واختبار وصلات يفترض اتصالها، لأن لقطة اللعبة تثبت فجوة بين أحرف قابلة للوصل؛ لا يعتمد الحكم على advance وحده، ولا يُعدل الخط قبل إثبات فجوة من الرسم أو الموضع.
- [ ] قياس الفجوة النظامية لكل أشكال العربية المتصلة في حزمة Barlow الجديدة: مقارنة نهاية الحبر وبداية الحبر مع `HorizontalAdvance` وbearing لكل شكل، ثم إعداد إصلاح موحد فقط إن ثبتت فجوة عامة بين كل الحروف القابلة للوصل.
- [ ] تحديد النسخة الأصلية غير المعدلة من حزمة LumenTale المطلوبة للاختبار وإرسالها للمستخدم: غير متاحة في مساحة العمل الحالية خارج مجلد الرفع، لذلك يلزم الحصول عليها من تثبيت اللعبة الأصلي أو من نسخة المستخدم قبل الإرسال.
- [x] تشخيص إصابة رموز LumenTale التقنية أثناء بناء Bundle النصوص: السبب هو دخول وسوم الزاوية، ومنها `<pause=1>`، إلى `reverseBidi` بعد اجتياز الفحص العميق، فتنعكس وتظهر كنص. الباني الآن يمنع الكتابة إذا اختلف تسلسل الرموز بعد التشكيل أو بعد إعادة فتح الناتج.
- [x] إصلاح فساد وسوم LumenTale أثناء تشكيل العربية: يُعزل الآن كل رمز مطابق لحارس LumenTale قبل التشكيل ويُعاد بالتسلسل نفسه بعده. نجحت 14/14 من اختبارات LumenTale المتاحة وبناء Vite؛ اختبارات إعادة فتح Bundle الفعلية بقيت متخطاة لأن الحزمة الأصلية غير متاحة في مساحة العمل.
- [x] مراجعة جودة شاملة لمحرر LumenTale وأدواته: فُحصت مسارات الاستخراج والتحرير والتحسين وحارس الرموز والبناء، وصُححت مخالفات موضعية واختُبرت 50 حالة مستهدفة بنجاح مع 4 اختبارات Bundle متخطاة لغياب الحزمة الأصلية؛ توثيق الأدلة والفجوات المتبقية في `docs/lumentale-editor-quality-audit.md`.
- [x] إضافة قسم مستقل للعبة GTA IV: أضيفت صفحة فحص محلية لـGXT/OXT بعد تحديد الملفات الفعلية، من دون افتراض صيغة أو تعديل موارد.
- [x] المسار النشط للإنجليزية: فُصلت صفحة GTA IV عن مود الروسية وتعتمد `american.gxt` و`fonts.dat` المرفوعين كمصدرين فقط؛ لا تستعمل `russian.gxt` أو `fonts_r.dat` في الترجمة الإنجليزية الجديدة.
- [x] إنشاء فهرس قراءة إنجليزي مستقل: يعرض النص الإنجليزي والجداول والمفاتيح والرموز التقنية من `american.gxt`، مع بقاء البناء معطلاً إلى أن تثبت طريقة عرض العربية لخط الإنجليزية. تحقق TypeScript و8 اختبارات GXT/OXT وبناء Vite المباشر نجحوا، كما تم تحميل `american.gxt` فعلياً في الواجهة والتحقق من البحث عن `BRAKE` وإظهار 30 نتيجة مطابقة مع CRC والجداول.
- [x] تحديد مسار عرض العربية لخط الإنجليزية: فُحصت حاويات خطوط English ومورد الرسم العربي، وثبت تطابق بنية `RSC5` وأسماء الخامات الداخلية وأحجام الموارد المفكوكة. أُنشئت حزمة اختبار مستقلة بأسماء English فقط، ولا تحمل ملفات باسم Russian.
- [x] بدء تنفيذ مسار الإنجليزية المعزول: أُخرجت حزمة عمل مستقلة تضم `american.gxt` غير المعدل و`fonts.dat` و`fonts.wtd` و`fonts_streamed_1.wtd` بأسماء English الطبيعية، وتحققت بصمات كل ملف وسلامة الأرشيف. لم تُنشأ ترجمة عربية للنص بعد.
- [x] دمج GTA IV في محرر الأداة الحالي: تحميل `american.gxt` وعرض كل السطور الإنجليزية مع حقل ترجمة عربي لكل هوية `(table, CRC)`، وبطاقات وفلاتر للأقسام، وحفظ الترجمات في ملف منفصل، ورفض أي ترجمة تغيّر رموز `~...~`. لا يُنشأ محرر منفصل ولا يُبنى GXT أو يكتب أي ملف لعبة في هذه الخطوة. تم اختبار تحميل المصدر الحقيقي: 524 جدولاً و59,637 سطراً، وظهرت البطاقات داخل المحرر، وقسم البناء مخفي لجلسة GTA IV.
- [ ] إعادة قسم البناء إلى جلسة GTA IV داخل المحرر: يبقى ظاهراً ولا ينشئ ملف GXT عربي حتى تُثبت خريطة الترميز والبناء الآمن؛ تعرض الواجهة السبب بدلاً من إخفاء القسم.
- [ ] تعديل خط English المرفوع: استخدام مورد الخط العربي كمرجع لبنية خانات العربية فقط، وتعديل `fonts.dat` و`fonts.wtd` و`fonts_streamed_1.wtd` في نسخة عمل منفصلة بأسماء English، ثم ربط خريطة الخانات ببناء `american.gxt`. لا تُعدّل الملفات الأصلية أو أي ملف باسم Russian.
- [ ] استخراج خريطة تحويل العربية من المود نفسه بصورة قابلة للتحقق: ربط العربية المنطقية وأشكالها بوحدات GXT الموجودة، مع اختبار عكسي على النصوص العربية العاملة؛ ممنوع الاعتماد على OCR أو تخمين بصري للخط وممنوع تعديل `fonts_r.dat` أو `fonts_r.wtd`.
- [ ] اشتقاق مرشحات خريطة العربية من الـ20,048 هوية المطابقة للمرجع الروسي: لا يعتمد أي رمز إلا عند توافق متكرر وقابل للذهاب والإياب، ويظل أي رمز غير مثبت خاماً ولا يُعرض للمستخدم كعربية. **الحالة:** متوقفة؛ OXT وMAP لا يحملان أسماء عربية، وقلب MAP مع صفحات الترميز القياسية لم ينتج نصاً صحيحاً.
- [ ] تفعيل تصحيح شامل لترجمة GTA IV فقط: بعد إثبات خريطة التحويل، تُضاف واجهة لتحرير كل السطور وإخراج `russian_fixed.gxt` جديد مع حفظ هوية `(table, CRC)` ورموز `~…~` وترتيب جداول ومفاتيح GXT. لا يُعدل المصدر المرفوع أو أي ملف خط ضمن هذه المهمة.
- [x] فحص `common/text/american.gxt` من GTA IV الأصلية للعبة الأساسية: كل هوياته البالغة 59,637 موجودة في ملف المود، ولذلك صار مرجع المعنى الأكثر اكتمالاً؛ لا يبدل خانة الروسية أو خطها العربي.
- [x] فحص `fonts_r.dat` الأصلي المطابق لـ`russian.gxt`: تعليقات MAP تثبت خريطة الروسية وتفك النص إلى سطور مقروءة؛ لم يحتج هذا الفحص إلى `fonts.dat`.
- [x] فحص `russian.gxt` و`fonts_r.dat` المرفوعين حديثاً للقراءة فقط: الملف GXT صالح (524 جدولاً و59,633 مدخلاً)، وMAP الروسية تفك عينات فعلية إلى روسية مقروءة؛ سيستعملان كمرجع معنى/هوية فقط، لا كخريطة عربية.
- [x] فهرسة `مجلدجديد(2).zip` للقراءة فقط: يحتوي `american.gxt` و`fonts.dat` وملفي WTD إنجليزيين فقط، بلا أداة أو جدول تحويل. كل مفاتيح `american.gxt` البالغ عددها 59,637 تطابق مفاتيح المود؛ هو مرجع معنى شامل لا بديل لمسار الروسية أو خطها العربي.
- [x] فحص `russian.gxt` و`russian.oxt` للقراءة فقط: كلاهما يمثل لغة GTA IV نفسها؛ GXT ثنائي Version 4/CharSize 16 فيه 524 جدولاً و59,736 مدخلاً، وOXT تصدير نصي قابل للتحرير يحمل الجداول والمدخلات نفسها. طابقت البيانات الخام لجميع 25,809 مفاتيح CRC الرقمية المشتركة (0 اختلاف)، ولذلك OXT ليس ملف لغة إضافياً مستقلاً ولا يحمّله محرك اللعبة مباشرة.
- [x] إنشاء قسم GTA IV الأولي: أضيفت صفحة `/gta-iv` وبطاقة في الرئيسية لفحص GXT/OXT محلياً. يتحقق القارئ من Version 4 وCharSize 16 وTABL/TKEY/TDAT وإزاحات النصوص من دون فك ترميز أو كتابة.
- [x] فهرسة أرشيف مود GTA IV المرفوع للقراءة فقط: حُددت ملفات اللغة والخط للعبة الأساسية وTLAD وTBoGT، وثبت أن الخط العربي موجود بالفعل. لم يُعدل أي مورد خط أو أرشيف.
- [x] تحديث رسائل فاحص GTA IV: توضيح أن المرجعين الروسي والإنجليزي فُحصا وأن العائق المتبقي هو خريطة تحويل العربية الخاصة بالمود، لا غياب ملفات الخط عموماً؛ لا يضاف محرر كتابة قبل إثبات الخريطة.
- [x] ربط هوية نص GTA IV للقراءة فقط: يقرأ القارئ وحدات GXT/OXT الخام ويحل المفتاح المسمي بخوارزمية GTA IV إلى `(table, CRC)`، مع حارس يحفظ رموز `~…~` بالترتيب والقيمة ويرفض `~` المفردة. تحقق الفحص الفعلي من 524 جدولاً و59,736 مدخلاً: جميع الهويات والوحدات الخام متطابقة بين GXT/OXT. لا توجد واجهة تحرير أو إدخال عربي حر في هذه المرحلة.
- [x] إثبات إعادة بناء GXT المحافظة: النسخة بلا استبدالات مطابقة بايتياً لـ`russian.gxt` المستخرج، وتعديل وحدة خام موجودة في الذاكرة يعاد فتحه مع حفظ الجداول وCRCs وكل وحدات السطور غير المستهدفة. الباني لا يقبل إدخال Unicode العربي الحر ولا يخرج ملفات خطوط أو يختبر داخل اللعبة.
- [x] فحص قواعد أداة تحسين الترجمة القابلة للتشغيل والإيقاف: القواعد المفعلة تُحفظ محلياً وتُرسل إلى `enhance-translations`، وتدخل في البرومبت وتُفلتر النتائج بعد رد النموذج للأنواع المبنية المعروفة. الحماية الصارمة لرموز LumenTale مستقلة ولا تتعطل بإيقاف مفتاح الوسوم. الفجوات المثبتة: قواعد الحماية غير المتعلقة بالرموز تعتمد غالباً على التزام النموذج بالنص الموجّه، والأنواع الناتجة من القواعد المخصصة لا تُربط بفلتر خادمي دقيق عند تعطيلها.
- [x] إضافة برومبتات وقواعد تحسين خاصة بـGTA IV: يمرر المحرر `game: gtaiv` إلى مسار التحسين، ويوجه النموذج إلى العربية المناسبة للواجهة والحوارات، ويرفض الاقتراح الذي يغير قيمة أو ترتيب رموز `~...~` في الواجهة والخادم. بقيت مسارات الألعاب الأخرى معزولة عن هذه القاعدة.
- [x] تدقيق وإعادة توزيع فلاتر GTA IV: المصدر الحقيقي كان يضع 51,327 من 59,637 في «أخرى». أُعيد التصنيف وفق دليل الجداول: 28,449 جدول `*AUD` للحوار/المشاهد، `CREDIT` للاعتمادات، معرّفات مثل `FM2_BE` والحمولات الرقمية في فلتر داخلي، وباقي MAIN للعب والعالم؛ النتيجة لا تحتوي أي فئة «أخرى». الأعداد المتحققة: الحوار 24,774، المعرّفات 18,258، اللعب والعالم 5,834، نصوص العالم والمتفرقات 5,392، الاعتمادات 1,723، والبقية موزعة على الواجهة والتحكم والنظام والعناصر والإعلام والمهام.
- [x] إصلاح فاحص الوسوم التقنية: حُذفت فئة الكلمات العادية المكتوبة بحروف إنجليزية كبيرة من المحرك والواجهة، لذلك لا تظهر كلمات مثل `NEW` و`GAME` و`EXP` كوسوم. بقيت الوسوم الحقيقية ذات الأقواس والمتغيرات وتسلسلات الهروب وأحرف التحكم ومحددات التنسيق. نجحت اختبارات الفاحص (12/12) وTypeScript وفحص diff.
- [x] تدقيق عميق لفاحص الوسوم التقنية وتقرير نتائجه: أضيفت فئة رموز التحكم `#0` إلى `#5` التي كان حارس البناء يحميها ولم يعرضها التقرير، وضُيّقت قاعدة `{...}` كي لا تعتبر التعليقات العادية متغيرات، وصُحح تداخل الوسوم كي تظهر الوسوم HTML داخل الوسوم المزدوجة بدل إخفائها. التقرير الآن يعرض اسم النوع وسبب الحماية وسياقاً محدداً يحيط بالوسم ومسار السطر، وتصدير JSON يحمل العينات نفسها. نجحت 15/15 من اختبارات الفاحص وTypeScript والبناء وفحص diff.
- [x] تدقيق حماية الوسوم والفحص العميق لـGTA IV من التقرير المرفق: حارس `~...~` هو المرجع نفسه في التحسين والحفظ والبناء والفحص العميق. تقرير الوسوم لا يصنف في جلسة `gtaiv/` الأسعار مثل `$100` و`$20m` أو العناوين مثل `#1 rated` و`Funk #49` كوسوم تقنية. الفحص العميق يصدر فئة حرجة متخصصة عند اختلاف رموز `~...~`، ويصلح فقط تبديل قيمة/ترتيب الرموز الكاملة عندما تتطابق المواضع؛ أما الرمز الناقص أو الزائد أو المنفرد فيبقى للمراجعة ولا يعيد النص الإنجليزي. نجحت اختبارات GTA IV وفاحص الوسوم والفحص العميق (38/38) وTypeScript والبناء وفحص diff.
- [x] بحث خيارات قانونية مجانية لتشغيل محركات الترجمة داخل أداة LumenTale: المسار الموصى به هو LibreTranslate/Argos مستضاف ذاتياً للترجمة الأساسية بلا تكلفة استخدام وبخصوصية أعلى، مع مزود `libretranslate` يوجّه إلى عنوان المستخدم. لا يُعتمد مسار Google الحالي المبني على نقاط عامة غير موثقة؛ البديل الرسمي Google Cloud Translation يقدّم أول 500,000 حرف شهرياً مجاناً لكنه يتطلب مشروعاً وفوترة. نتائج البحث محفوظة في `/home/ubuntu/free-translation-research.md`.
- [x] إعداد مورد TMP عربي تجريبي خارج الحزم الأصلية مع Atlas وجدول محارف، ثم فحصه بإعادة الفتح: أضيفت 365 محرفاً من Noto Naskh Arabic إلى صفحة Atlas الحرة مع بقاء هوية المورد والمحارف السابقة كما هي.
- [x] ربط مورد العربية التجريبي فقط بعد تدقيق البنية ومقارنة الفروقات، ثم تسليم الحزمة للاختبار داخل اللعبة: حافظت Barlow على مراجعها الحالية إلى DroidSansFallback؛ 198/198 محارف عربية لازمة للمسار جرى التحقق منها، وفروق Atlas خارج مستطيلات العربية = صفر.
- [x] فحص `عربي.bundle` المرفوع وقياس فرق الحجم والضغط والموارد مقارنة بحزمة LumenTale الأصلية، قراءةً فقط: بيانات المورد الخام زادت 92 بايتاً فقط، لكن الباني أعاد ضغط الحزمة كاملةً بـLZ4 بدلاً من LZ4HC الأصلي؛ لذلك زاد حجم الملف 143,377 بايتاً.
- [x] تعديل باني LumenTale ليحافظ على ضغط LZ4HC وبيانات ضغط الفهرس الأصلية عند إنشاء Bundle جديد.
- [x] بناء Bundle تجريبي بعد الإصلاح، ثم التحقق من إعادة الفتح وثبات الهوية والترجمة والحجم المضغوط: فُتحت الحزمة في عملية ثانية، وثبتت 30 جدولاً و14,476 سجلاً، واستقر الناتج عند 457,234 بايتاً بدلاً من 592,520 بايتاً مع LZ4 العادي.
- [x] إضافة اختبار يمنع رجوع باني LumenTale إلى LZ4 العادي في أي تعديل لاحق.
- [x] عزل سبب تعطل اللعبة عند وضع حزمة خط العربية التجريبية: لم تفقد الحزمة القديمة كائنات TMP أو مسارات موارد، لكنها خرجت بلا ضغط UnityFS (`None`) بدلاً من LZ4HC؛ عُزل كاتب الضغط كسبب بنيوي مرجح للتعطل قبل إنشاء اختبار جديد.
- [x] بناء Bundle خط LumenTale عربي مرشح: استُخدم الكاتب المحلي ومسار ضغط Atlas، وأعيد فتح الناتج محلياً مع مطابقة ظاهرية للكائنات والمسارات وترويسة LZ4HC. هذا تحقق محلي فقط وليس دليلاً على قبول محرك اللعبة.
- [x] تجربة Bundle الخط المرشح داخل LumenTale: ما زالت اللعبة تغلق سريعاً. لا يعتبر إصلاح الخط ناجحاً، ويظل العطل مستقلاً عن Bundle النصوص.
- [ ] تحديد لماذا تظهر مفاتيح القوائم مثل `NEWGAME_OPTIONPROMPT` بدلاً من النصوص، ومقارنة صفوف جدول الواجهة بين الأصل و`عربي.bundle` قراءةً فقط.
- [ ] عزل مشكلة مفاتيح القوائم مع استعادة الخط الأصلي: التحقق من صفوف الواجهة ومسار جدول اللغة فقط، من دون أي تعديل لملف الخط.
- [ ] البحث في المصادر العامة عن أي ترجمة غير رسمية منشورة للعبة LumenTale: Memories of Trey وتوثيق النتيجة.
- [x] فحص `عربي.bundle` الجديد الذي رفعه المستخدم قراءةً فقط: حجمه 592,494 بايت (نحو 578.6 KiB)، وضغطه `None` بدلاً من LZ4HC؛ تعلن ترويسة الأصول 1,049,000 بايت مقابل 1,049,004 بايت فعلياً، لذلك تُصنّف كمورد ولا تحتوي جداول أو `UI_en`. ليست ناتجة عن مسار البناء المصحح، الذي اجتاز اختبار LZ4HC وإعادة فتح 30 جدولاً من الأصل.
- [x] مانع حرج أول: `عربي.bundle` المفحوص حجمه 592,494 بايت بلا ضغط وترويسة أصول ناقصة بأربعة بايتات؛ لذلك يُصنّف Resource ولا يمثل ناتج المسار المحلي الجديد.
- [x] بناء حزمة نصوص فحص مستقلة من الأصل عبر المسار المصحح: تعديل قيمة واجهة إنجليزية واحدة فقط، ثم تحقق مستقل من LZ4HC وترويسة Assets و30 جدولاً و`UI_en` ذات 591 صفاً؛ الناتج 457,232 بايت.
- [x] بناء حزمة نصوص فحص مستقلة ذات 457,232 بايت مع LZ4HC وتعديل واجهة إنجليزية واحدة؛ لم تُجرّب داخل اللعبة، لذا لا تمثل دليلاً على التوافق أو سبب الإغلاق.
- [x] فحص `عربيجديد.bundle` المرفوع حديثاً بوصفه ناتج زر البناء الفعلي الذي أغلق اللعبة: 457,276 بايت، UnityFS LZ4HC، وAssetFile سليم ظاهرياً يحوي 31 كائناً و30 جدولاً.
- [x] تحليل AssetFile الثنائي: بقيت metadata وهيكل TypeTree ومحاذاة البيانات ثابتة؛ تغيّر كائن `UI_en` وحده (8 سطور `m_Localized`، +60 بايت) مع بقاء الكائنات الثلاثين الأخرى مطابقة خاماً للأصل.
- [x] ثبت بفك LZ4 مستقل خارج `unityfs-js` أن كتل البيانات 0–2 في `عربيجديد.bundle` غير صالحة معيارياً؛ الأصل يفك بالكامل، بينما الملف المبني يفشل في الكتل الأولى. هذا سبب تقني مثبت مرجح لإغلاق Unity، وليس نقص الخط العربي أو تعديل الجداول الأخرى.
- [x] إصلاح ضاغط كتل بيانات LZ4HC المحلي: كان يحتفظ بخمسة بايتات نهائية فقط، ما سمح بأن يبدأ آخر تطابق بعد الحد الذي تقبله مفككات LZ4 القياسية. صار يحتفظ بثمانية بايتات، ويضع حارساً بنيوياً مستقلاً قبل إدراج أي كتلة مضغوطة.
- [x] تحقق داخلي عبر مصدر unityfs-js المحلي الذي تستعمله الأداة: حزمة بسطر `UI_en` واحد خرجت 457,248 بايت، حافظت على UnityFS LZ4HC و9 كتل، وفكها مدقق LZ4 مستقل كاملاً. اجتاز تطبيق الأداة `pnpm build` بعد الإصلاح.
- [x] تحقق المستخدم من Bundle النصوص المبني بعد إصلاح LZ4: اللعبة تعمل وتعرض مربعات مكان النص العربي، ما يثبت قبول محرك اللعبة للبنية الجديدة وأن غياب أشكال الخط العربي فقط هو سبب المربعات.
- [x] فحص Bundle الخط الذي أغلق اللعبة بمدقق LZ4 مستقل ومقارنته بايتياً بالخط الأصلي في metadata والكائنات والكتل والـAtlas قبل بناء أو طلب أي ملف خط جديد.
- [x] فحص Bundle الخط المرشح بفك LZ4 مستقل: الخط الأصلي (10,579,884 بايت) يفك كاملاً، أما `team17_fonts_assets_barlow-semicondensed_arabic-naskh-lz4hc-validated.bundle` الذي أُغلق اللعبة معه (10,794,559 بايت) فيحتوي 28 كتلة بيانات LZ4 غير قابلة للفك، من 183 كتلة. العطل هو مسار ضغط Bundle الخط السابق، وليس Atlas العربية أو النصوص.
- [x] إعادة بناء Bundle الخط من الأصل باستخدام ضاغط LZ4HC المصحح نفسه الذي ثبت نجاحه في Bundle النصوص، ثم فحص جميع الكتل مستقلاً ومقارنة جميع الكائنات غير المعدلة قبل أي تجربة لعبة.
- [x] إعادة بناء Bundle الخط من الأصل بالضاغط المصحح: أُنتج `team17_fonts_assets_barlow-semicondensed_arabic-naskh-lz4hc-fixed.bundle` (10,794,915 بايت) من Atlas العربية نفسها.
- [x] تدقيق Bundle الخط المعاد: فُكت 183/183 كتلة بنجاح بمدقق LZ4 مستقل (صفر كتلة تالفة)، وبقيت 78/80 كائناً مطابقة للأصل؛ الكائنان المتغيران فقط هما أصل `DroidSansFallback SDF` وTexture2D الخاص بـAtlas الصفحة 3، ولا توجد فروق Atlas خارج مستطيلات العربية المخصصة.
- [x] تحقق المستخدم من Bundle الخط السليم: اللعبة تعمل وتظهر العربية، ما يثبت أن ضغط Bundle وبنية مورد TMP مقبولان للمحرك.
- [x] فحص سبب انقلاب الحروف العربية رأسياً: ثبت أن مولد Tiles يخرج الصفوف من أعلى لأسفل بينما Texture2D يقرأها بالعكس؛ عُكس ترتيب الصفوف داخل كل مستطيل عربي فقط مع بقاء GlyphRect ثابتاً.
- [x] فحص وضوح خط القوائم: خُصص `DGShamael-Black_Fixed.ttf` للرستر العربي بمقاس 52، وهو أكبر من إعداد Noto السابق ويحافظ على 196 شكلاً عربياً داخل مساحة Atlas الموجودة.
- [x] عزل مسار تشكيل العربية: لا ينفذ محرك LumenTale تشكيل العربية؛ يجب حفظ أشكال العرض العربية مسبقاً وباتجاه مناسب للمحرك.
- [x] إصلاح مرئي مطلوب: بُني Atlas 3 جديد من `DGShamael-Black_Fixed.ttf` مع اتجاه Unity الصحيح. تغيرت بايتات Atlas العربية فقط، وبقيت أبعاد ومواقع المستطيلات وكائنات الـBundle ثابتة.
- [x] قبول خط القوائم: ينجح الرستر الجديد في مساحة Atlas المحجوزة بلا قص أو كتابة خارج المستطيلات؛ التحقق النهائي من الوضوح داخل القائمة يتطلب لقطة لعبة واحدة لأنه يعتمد على مقياس عناصر واجهة LumenTale.
- [x] إصلاح باني LumenTale: صار يشكل النص العربي الخام ويعكس اتجاهه قبل تسلسل `m_Localized`، ويحافظ على النص المشكل مسبقاً كما هو. اجتاز اختبار Bundle بعبارة عربية و`pnpm build`.
- [x] عزل تعارض مسار التصدير الفعلي: سبب ناتج `None` والترويسة الخاطئة هو أن Lovable/Bun لا يطبّق رقعة `pnpm`؛ استبدل الاستيراد بمصدر UnityFS محلي مصحح يمر عبر بناء Vite.
- [x] عزل تعارض مزامنة Lovable: ظهر الالتزام السابق في سجل GitHub لكن واجهة التطبيق بقيت على 1.181.9 لأنها لم تتضمن تفاصيل الإصلاح؛ أُضيف إصدار وسجل تغيير ظاهرين مع الإصلاح الفعلي.
- [x] إضافة رقم إصدار جديد وسجل تحديث ظاهر في Lovable يشرح إصلاح ضغط LZ4HC لبناء LumenTale والتحقق من بنية الحزمة قبل التنزيل، ثم دفعه إلى GitHub في `07f1da2`.
- [x] نقل إصلاح LZ4HC وترميز أعلام UnityFS من رقعة `pnpm` إلى مسار مصدر يضمن أن Lovable/Bun يطبّقه عند بناء المتصفح؛ اجتاز اختبار بناء الحزمة الأصلية وإعادة فتح 30 جدولاً وضغط LZ4HC.

- [x] إرجاع تعديل البناء القسري غير المطلوب إلى حالته السابقة غير المنشورة.
- [x] فحص `Font.arabic.arc` ومسار إدراجه في BBS0 وBBS1 فقط، من دون أي تعديل أو بناء أو نشر قبل عرض النتيجة للمستخدم.
- [x] إصلاح اختيار الأشكال الأولية في مولد `Font.arabic.arc` وإعادة بناء ملف الخط فقط.
- [x] تدقيق الخانات الـ126 ومعاينة الخط الجديد قبل أي إدراج في BBS أو ISO.
- [x] إضافة `Font.arabic.arc` المصحح إلى الأداة وربطه فقط بإدراج الخط في BBS0 وBBS1 عند البناء.
- [x] اختبار شيفرة الأداة ونطاق التعديل من دون بناء BBS أو ISO أو تغيير النصوص.
- [x] رفع رقم الإصدار وسجل التغييرات لميزة الخط العربي المصحح المضمّنة.
- [x] إعادة اختبارات البناء وBBS وISO للإصدار 1.181.1 من دون بناء أي BBS أو ISO للمستخدم.
- [x] نشر التحديث المتفق عليه فقط بعد التحقق النهائي من فرق الملفات.
- [x] تشخيص زر «استخدام الخط العربي» الذي لا يعطي نتيجة ظاهرة وإصلاحه وحده.
- [x] اختبار تحميل الخط المضمّن في المتصفح مع وصول ملف ARC الكامل من نسخة الإنتاج.
- [x] رفع الإصدار وسجل التغييرات ونشر إصلاح زر الخط فقط.
- [ ] مقارنة مورد الخط داخل BBS0 وBBS1 الناتجين بالـ `Font.arabic.arc` المصحح لإثبات هل دخل البناء فعلياً.
- [ ] حصر موارد الخط وجداول عرض المحارف التي تستعملها القوائم والحوارات قبل أي تعديل جديد.
- [ ] تقديم دليل السبب وخطة محددة للمستخدم قبل تعديل أي ملف خط إضافي.
- [x] تدقيق فرضية سجلات `mesfont.inf` و`mesfont.clu` السابقة؛ ثبت أنها بُنيت على قراءة فهرس ARC خاطئة وأن `clu` لوحة ألوان وليست جدول محارف.
- [x] تثبيت بنية Font.arc الحقيقية: `mesfont.inf` ترويسة، و`mesfont.mtx` نسيج 4-bit، و`mesfont.clu` لوحة، و`mesfont.cod` جدول 378 سجل محرف.
- [x] تدقيق مسار الأكواد العربية في `mesfont.cod` الفعلي وتحديد 126 سجلاً وخانة مستقلة بلا تداخل مع أكواد CTD الإنجليزية المفحوصة.
- [x] إلغاء مسار إصلاح `mesfont.clu` غير الصحيح قبل تطبيقه؛ لم يُعدل جدول اللوحة على أساس افتراض خاطئ.
- [x] إعادة تدقيق الخط المبني: 126/126 خلية مرسومة وسجل صحيح، ولا تغيير خارج `mesfont.mtx` و`mesfont.cod`.
- [x] إدراج Font.arabic.arc المتحقق في الأداة، وبناء المشروع، ورفع الإصدار وسجل التغييرات بعد اجتياز الفحص.
- [x] فحص مورد `mesfont.mtx` الحقيقي (نسيج 4-bit بحجم 512×1024) وتحديد 127 خانة فارغة 18×18، منها 126 مستخدمة بأمان.
- [x] فحص مورد `mesfont.cod` الحقيقي (378 سجل محرف) وتحديد سجلات الاستبدال العربية بلا تغيير حجم الجدول.
- [x] إعداد خريطة عربية لخانات 18×18 داخل النسيج الحقيقي، مع تدقيق عدم التداخل وتخصيص 126 خانة مستقلة.
- [x] بناء `Font.arabic.arc` جديد من `Font.arc` الإنجليزي الأصلي بتعديل `mesfont.mtx` و`mesfont.cod` فقط، ثم تدقيق فرق بايتي مستقل ومرئي.
- [x] إدراج الخط المبني من الموارد الصحيحة فقط في الأداة بعد إثبات القراءة والرسم والسجلات؛ اجتازت 14/14 اختبارات Kingdom Hearts المستهدفة.
- [ ] تدقيق وصول التزام إصلاح الخط إلى `origin/main` ورابط المستودع الظاهر للمستخدم، ثم تصحيح المزامنة إن لزم من دون تغيير المحتوى.
- [ ] تحديد ربط مشروع Lovable الظاهر للمستخدم مع GitHub أو ملفات المصدر، ثم مزامنة إصدار 1.181.3 والتحقق من ظهوره في صفحة Lovable.
- [ ] التحقق داخل Lovable من وصول التزام `a33fd13` إلى مسودة المشروع وربط الفرع `main`، ثم طلب نشره فقط إذا كانت المسودة غير منشورة.
- [ ] إنشاء دفعة مستقلة تغيّر رقم الإصدار فقط في `src/lib/version.ts`، ثم دفعها إلى `main` والتحقق من المرجع البعيد.
- [ ] تحديد مورد الخط الفعلي لحوارات الشاطئ ومورد الخط الفعلي لشاشات الإيقاف والأوامر من ملفات BBS والباتش، بالقراءة فقط.
- [ ] قياس سماكة وعروض ومحاذاة أشكال العربية في الخط الحالي مقابل مظهر حوار الشاطئ، وإعداد خطة تحسين مثبتة.
- [ ] تحديد سبب الرموز الغريبة في القوائم قبل تعديل أي خط أو خريطة كود ثانية.
- [ ] تشخيص تقطّع بكسلات الحوار العربي الظاهر في تشغيل PPSSPP بمقارنة قيم نسيج `mesfont` وشفافيته مع حروف إنجليزية سليمة، قراءةً فقط.
- [ ] بناء نموذج بديل للخط بعد إثبات ترميز البكسلات الصحيح، ثم اختباره في اللعبة قبل تضمينه أو دفعه.
- [x] تتبع أشكال العربية داخل كلمة من التشكيل إلى تسلسل أكواد CTD، والتحقق من أن اختيار شكل المفرد/الأول/الوسط/الآخر والترتيب صحيحان؛ سبب الانفصال هو توسيط الحبر داخل الخانة بدلاً من محاذاته بعرض التقدم.
- [x] إعداد نموذج محاذاة محدود لانفصال الحروف؛ يبدأ حبر الأشكال من أصل الخانة ويصل نطاق الاتصال إلى عرض التقدم للأشكال المتصلة، ومدقق خارج الأداة قبل التضمين.
- [x] قياس عرض سجلات `mesfont.cod` والمسافة الفعلية بين كل زوج من الأشكال العربية في كلمات تشغيلية، ثم تحديد ما إذا كان اتصال الحروف يتطلب تغيير العرض أو الرسم أو كليهما.
- [x] مقارنة لقطة PPSSPP حديثة للكلمات المتقطعة مع نموذج `Font.arabic.arc` الحالي لتحديد إن كان الخلل من رسم المحرك أو مزج/شفافية النسيج، بالقراءة فقط.
- [x] قياس تكسّر حبر الحرف العربي نفسه وتوزيع تدرجاته في لقطة PPSSPP مقابل نسيج `mesfont`، من دون تغيير العرض أو CTD أو أي ملف BBS. النتيجة: لا يشكّل فهرس 3 القوي سوى 15.04% من الحبر؛ 116 من 126 شكلاً تحتوي جزر حبر قوية متعددة.
- [x] إنشاء عينة Raster خارجية للأشكال FE80 وFE85 وFE8C وFE90 وFEA0 وFEB9 وFEC0 وFEC8 وFEE3 وFEF4، ومقارنة النماذج A–D بقياس جزر فهرس 3 والحدود داخل 18×18 قبل إعادة بناء الخط كاملاً.
- [x] بناء `mesfont.mtx` و`menufont.mtx` تجريبيين بالنموذج D خارج Font.arc، وتدقيق 126 شكلاً: الحدود، الجزر الصغيرة، نسب فهرس 3، والمطابقة البايتية خارج مناطق الأشكال.
- [x] فحص هندسة `menufont.inf` وسجلات `menufont.cod` وبناء نموذج D متكيف مع حجم خليته الأصلي قبل إعادة تشغيل التجربة الخارجية الكاملة. النتيجة: خلايا menufont أصلية 14×14، ونجح التدقيق المستقل للعائلتين.
- [x] دمج `mesfont.mtx` و`menufont.mtx` التجريبيين D فقط في نسخة `Font.arabic.arc` جديدة، ثم تدقيق البنية والفرق البايتي قبل تضمينه في الأداة. النتيجة: 7,362 بايت متغير داخل MTX المستهدفين فقط، وكل الفحوص البنيوية والبايتية اجتازت.
- [x] تضمين نسخة D المدققة في مورد الأداة وتشغيل اختبارات Kingdom Hearts المحددة. النتيجة: 9/9 اختبارات نجحت وTypeScript بلا أخطاء؛ إخفاقات المجموعة الكاملة تخص اختبارات Mother 3 غير المرتبطة.
- [x] إصدار ودفع النسخة 1.181.8 إلى `main` بعد اجتياز اختبارات Kingdom Hearts وبناء الإنتاج. الدفعة: `8ff0841`.
- [x] إنشاء عينات D1 وD2 وD3 لعشرة أشكال عربية فقط ومقارنة التحجيم والتكميم وتوزيع التدرج قبل إعادة بناء أي نسيج كامل أو ملف BBS. لم يُكتب Font.arc أو MTX أو COD/CLU/CTD.
- [x] بناء نسختي `Font.arc` اختباريتين D2 وD3 بتعديل خلايا الأشكال العشرة في `mesfont.mtx` فقط، مع الاحتفاظ بالبصمة الأصلية للمقارنة.
- [x] تدقيق نسختي D2 وD3: ثبات حجم ARC وبنيته، وحصر الفروق في بايتات خلايا `mesfont.mtx` العشر دون أي تغيير في COD أو CLU أو CTD أو العروض أو النصوص. D2: 125 بايت MTX؛ D3: 348؛ صفر خارجهما.
- [ ] بعد استلام BBS0 وBBS1 الأصليين، بناء مخرجي BBS منفصلين لـD2 وD3 وتسجيل نتيجة اختبار PPSSPP لكل نسخة.
- [x] إضافة خياري اختبار D2 وD3 داخل الأداة لاختيار مورد الخط عند بناء BBS، مع إبقاء D1 الحالي هو الخيار الافتراضي.
- [x] اختبار مسار البناء لكل خيار: يدخل المورد المختار في BBS0 وBBS1 فقط، مع بقاء BBS2 وBBS3 مطابقين للأصل. الإصدار التجريبي جاهز للدفع من دون اعتماد D2 أو D3 كخط نهائي.
- [ ] تحديد ملف BBS الأصلي المتاح وبناء نسخة جديدة بالخط العربي 1.181.8 فقط، ثم تدقيقها وتسليمها لاختبار PPSSPP.
- [ ] مطابقة `Font.arc` داخل BBS0 أو BBS1 الذي استُخدم لإنتاج لقطة PPSSPP مع بصمة نموذج الإصدار 1.181.7، بالقراءة فقط.
- [ ] إعداد نموذج اتصال جديد بعد إثبات قياس العروض، واختباره على كلمات عربية متصلة قبل تضمينه أو دفعه.
- [x] حصر الموارد المتاحة والبحث عن ملفات الخط أو صفحات الحروف أو جداول عرض المحارف دون تعديلها.
- [x] تحليل المورد المرشح لتحديد صيغته وأبعاد المحارف وترتيبها والبيانات التي تربطه بنصوص CTD.
- [x] توثيق ما إذا كانت الحروف العربية يمكن إضافتها بأمان، وما الملفات الإضافية المطلوبة قبل بناء أداة حقن خط.

## مقارنة الخط الياباني الأصلي المرفوع

- [ ] تحليل ترويسة ومحتويات ملف `خطالعبةالياباني.arc` المرفوع من النسخة اليابانية الأصلية.
- [ ] مقارنة موارده وجداوله بملف `Font.arc` من الباتش الإنجليزي لتحديد خانات الحروف الآمنة للعربية.
- [ ] توثيق النتيجة وخطوة بناء أداة حقن عربية بتقرير واضح للمستخدم.

## تحليل Font.arc المرفوع

- [x] فحص ترويسة أرشيف الخط وفهرسه واستخراج موارده دون تغيير البايتات الأصلية.
- [x] تحديد صفحة الحروف والجداول ومقاييس العرض وترميز المحارف المستخدم في الحوارات.
- [x] توثيق إمكان استبدال أو إضافة العربية، ثم تحديد متطلبات أداة حقن خط آمنة داخل الأداة الأصلية.

## فحص ملفات Kingdom Hearts: Birth by Sleep

- [x] حصر محتويات أرشيف CTD وتحديد صيغ الملفات وأحجامها.
- [x] تحليل ترميز النصوص وبنية الفهارس أو الجداول إن وُجدت.
- [x] توثيق قابلية الدمج في أداة المستخدم والخطوة التقنية التالية دون تعديل الأقسام الحالية.

## قسم Kingdom Hearts: Birth by Sleep

- [x] بناء قارئ ومُعيد إنشاء CTD داخل المتصفح يحافظ على اسم الملف وبنيته وبايتات التحكم.
- [x] إضافة صفحة تفتح ملف CTD منفرداً أو أرشيف CTD وتعرض كل الملفات والنصوص للتحرير.
- [x] بناء أرشيف ZIP يحوي كل ملفات CTD المعدلة بأسمائها الأصلية.
- [x] اختبار ملف CTD تجريبي والتحقق من واجهة القسم على الهاتف.
- [x] دفع الالتزام المحلي لقسم Kingdom Hearts: Birth by Sleep إلى `main` والتحقق من المرجع البعيد.

## مدير ملفات Kingdom Hearts — أرشيفات DAT

- [x] مراجعة مدير ملفات Risen وتحديد مكوّناته وتدفق استخراج الملفات الذي يمكن نقله دون تعديل القسم الأصلي.
- [x] تحليل ترويسة وفهرس ملف DAT أصلي من Kingdom Hearts وتحديد طريقة عرض محتوياته أو حدوده بوضوح.
- [x] إضافة واجهة هاتفية لاختيار DAT واستعراض ملفات الأرشيف وتنزيل الملف أو كل الملفات المستخرجة.
- [x] اختبار فتح أرشيف BBSA اصطناعي سليم وفهرسة موردين وتنزيل مورد بحجم قطاع صحيح، ثم بناء التطبيق بنجاح.
- [x] حفظ التحديث ودفعه إلى `main`.

## فحص وفك تشفير ملفات BBS

- [x] إضافة زر «فتح في محرر النص» واضح لكل ملف CTD مكتشف من BBS0، وربطه بجسر محرر CTD مع اختبار أن المورد يفتح قابلاً للبناء.
- [ ] فحص أول بايتات موارد BBS الحقيقية لتحديد هل التشفير موجود، وما الملفات المتأثرة به.
- [x] توثيق خوارزمية فك التشفير المتوافقة مع نسخة Final Mix: مصدر DNAS يستعمل مفتاحاً ثابتاً ويستخرج Version Key من MAC عند `0x70` في BBS.
- [x] البحث عن مفتاح PGD منشور ومثبت لنسخة Final Mix، ومقارنته مع ترويسة BBS قبل اعتماده: تم تأكيد مفتاح DNAS الثابت من المصدر المرجعي.
- [ ] نقل خوارزميات KIRK وBBMac وBBCipher اللازمة لفك BBS1–BBS3 إلى TypeScript مع فحوص MAC كاملة.
- [ ] بناء مسار داخل مدير Kingdom Hearts ينشئ نسخة مفكوكة منفصلة ولا يغيّر ملف DAT الأصلي.
- [ ] تمرير ملفات BBS1–BBS3 المفكوكة مسبقاً كما هي من زر «فك BBS» بدلاً من رفضها كترويسة غير PGD.
- [ ] قبول BBS4.DAT في الفحص والفك، وتحديد هل يحتاج فكاً من ترويسـته بدلاً من رفضه بسبب الاسم.
- [ ] التقاط أول 0x100 بايت من ملف BBS الذي رفضه الزر، وتحديد ما إذا كان PGD في إزاحة أخرى أو صيغة BBS مختلفة قبل تغيير مسار التمرير.
- [ ] تحليل حاوية `bbs1.dat` ذات التوقيع المثبت `62 62 73 31`، وقراءة جدولها أو طول الترويسة لتحديد بداية PGD أو BBSA الفعلية قبل السماح بالفك أو الفتح.
- [x] إضافة زر «فحص BBS» محلي لملفات DAT من النسخة المطبق عليها الباتش، يقرأ أول 0x100 بايت ويعرض نوع الترويسة وبيانات HEX قابلة للنسخ من دون رفع الملف.
- [ ] اختبار أن النسخة المفكوكة قابلة للفهرسة وأن ملفات CTD وTIM2 تُقرأ منها بصورة صحيحة، ثم دفع التحديث إلى `main`.

## تنزيل ملفات DAT في مجلد واحد

- [x] مراجعة بناء ZIP الحالي الذي يحفظ المسار المجزأ لكل مورد.
- [x] إضافة خيار تنزيل مسطّح يضع الملفات المختارة كلها في مجلد واحد بأسماء فريدة.
- [x] اختبار ZIP المسطّح: موردان أصبحا داخل المجلد الجذر نفسه بلا مسارات مجهولة أو مجلدات متداخلة.
- [ ] دفع التحسين إلى `main` ثم اختبار رفع CTD حقيقي جماعياً في المحرر بعد تصحيح تمييز CTD.

## الوصول إلى نصوص Kingdom Hearts من DAT

- [ ] تحديد تجزئة مجلد نصوص CTD وامتداداته في فهرس BBSA الخاص بـBirth by Sleep.
- [ ] إظهار ملفات CTD كموارد نصية واضحة مع زر يوجّه إلى محرر Kingdom Hearts الموجود في الأداة.
- [ ] اختبار تنزيل CTD وفتحه في المحرر ثم دفع التحسين إلى `main`.

## تصحيح تمييز CTD في BBSA

- [x] تحليل برنامج الباتش وملفاته لتحديد الموارد التي يكتب إليها النصوص داخل BBS0–BBS3، ثم مطابقتها مع فهرس BBS قبل إعادة بناء فلتر CTD. النتيجة: ملفات الباتش CTD تبدأ فعلياً بـ`@CTD` وتُكتب في BBS0؛ يعتمد المدير الآن اكتشاف الترويسة المباشر مع مطابقة الفهرس الآمنة.
- [ ] مراجعة ترتيب جداول الامتدادات الفعلي في BBSA لأن المورد الموسوم CTD لا يحمل ترويسة CTD.
- [ ] التحقق من ترويسة الموارد قبل وسمها CTD وعرض نوع غير معروف عند غياب المطابقة.
- [ ] اختبار مورد نصي حقيقي في المحرر ثم دفع التصحيح إلى `main`.

## إصلاح مباشر لخطأ CTD الظاهر في الهاتف

- [ ] استخراج أول بايتات المورد `BBS3_9108070D_40D1D5FF.ctd` ومقارنتها بتوقيع CTD المقبول في المحرر.
- [ ] منع المدير من وضع الامتداد `.ctd` إلا عند تطابق الترويسة أو بعد تحقق داخلي من بنية CTD.
- [ ] تنزيل CTD مُثبت وفتحه في المحرر ثم دفع التصحيح إلى `main`.

## إصلاح ترويسة BBSA الحقيقية

- [x] تحديد أن الرفض سببه شرط قسمة غير صحيح في جداول الامتدادات، وتوثيق ترتيب حقول ترويسة BBSA وحدود القراءة.
- [x] تعديل قارئ الفهرس ليتعامل مع عدد مداخل الامتدادات الحقيقي بالقسمة الصحيحة واختبار فتح الفهرس وتنزيل مورد بحجم 2048 بايت.
- [ ] دفع التصحيح إلى `main` ثم إعادة اختبار BBS0.DAT الحقيقي من هاتف المستخدم.

## أسماء موارد Kingdom Hearts داخل DAT

- [ ] جمع جدول أسماء الموارد المعروف لـBirth by Sleep وربطه بتجزئات BBSA المعروضة في المدير.
- [ ] إضافة بطاقة Font.arc واضحة وبحث مباشر يعرض الاسم والمسار الحقيقيين عندما يكونان متاحين.
- [ ] اختبار عرض اسم ملف معروف وتنزيله ثم دفع التحديث إلى `main`.

## موضع الخط في Final Mix اليابانية

- [x] التحقق من غياب arc/system: لم يكن اختلافاً في Final Mix، بل تجزئة مقلوبة في خريطة مجلدات المدير.
- [x] تصحيح التجزئة وإضافة كشف بالمحتوى لمورد الخط عن طريق mesfont وcmdfont داخل ARC القابل للتنزيل.
- [ ] اختبار النتيجة على ملفات Final Mix الحقيقية ودفع التصحيح إلى `main`.

## ربط Kingdom Hearts: Birth by Sleep بالمحرر المشترك

- [x] تحليل عقدة بيانات المحرر وبانيه لتوصيل عدة ملفات CTD من دون مساس بأقسام Pokémon Emerald وRisen.
- [x] تحويل سجلات CTD إلى مدخلات المحرر المشترك مع المحافظة على اسم الملف ومعرّف السجل ووسوم التحكم.
- [x] إعادة بناء جميع ملفات CTD من ترجمات المحرر وتصديرها في أرشيف ZIP من قسم البناء المشترك.
- [x] اختبار الرفع ثم التحرير داخل المحرر ثم البناء وإعادة التحليل، ودفع الربط إلى `main`.

## تحليل أرشيف TIM2 المرفوع

- [ ] حصر ملفات TIM2 وتحديد صيغ الصور أو الخطوط أو اللوحات الموجودة داخل الأرشيف.
- [ ] التحقق من مورد قابل للتحرير ومسار دمج آمن داخل قسم Kingdom Hearts إن كان ذا صلة.

## فحص محرر صور Risen كنموذج مرجعي

- [ ] توثيق تدفق اختيار المورد والمعاينة والتنزيل والرفع والاستبدال والبناء في محرر صور Risen قبل نقل أي جزء منه إلى TIM2.
- [ ] فحص صفحة أداة صور Risen ومكتبات ximg الفعلية، بما يشمل التحويل إلى PNG ومواءمة الرفع وإعادة إدراج المورد داخل `images.pak`.

## أداة صور Kingdom Hearts TIM2

- [ ] بناء قارئ ومرمّز TIM2 داخل المتصفح يحافظ على الترويسة والفهرس ولوحة الألوان وترميز 8bpp.
- [ ] إضافة صفحة صور Kingdom Hearts لقبول ملفات TIM2 أو أرشيف ZIP وعرض معاينات قابلة للبحث.
- [ ] إضافة تنزيل PNG ورفع بديل مع مواءمة تلقائية للأبعاد والحفاظ على شفافية وصيغة المورد الأصلية.
- [ ] إضافة تحرير جزئي: اختيار منطقة، تصديرها، تنظيف النص الإنجليزي من المورد، ثم تركيب التعريب.
- [ ] بناء ZIP بملفات TIM2 المعدلة بأسمائها ومساراتها الأصلية، ثم اختباره على أرشيف TIM2 المرفوع.

## مطابقة أداة صور Kingdom Hearts مع أداة صور Risen

- [ ] مقارنة كل عناصر واجهة وتدفق أداة صور Risen مع أداة TIM2، وتسجيل ما يمكن نقله بأمان إلى TIM2 وما لا ينطبق بسبب اختلاف الصيغة.
- [ ] توحيد التخطيط والهاتفية وتسميات الأزرار ومسار: فتح المورد ← تنزيل الأصل ← رفع التعريب ← المواءمة ← المعاينة ← التراجع ← البناء.
- [ ] إضافة كل أداة صور مكافئة يمكن تنفيذها في TIM2 من دون تغيير اللوحة أو الترويسة أو الموارد غير المعدلة.
- [ ] اختبار المطابقة على أرشيف TIM2 الأصلي، وتوثيق حدود الصيغة بوضوح ثم دفع التحديث إلى `main`.

## تحسينات بطاقة Kingdom Hearts وإزالة جزء من الصورة

- [x] تكبير بطاقة صور Kingdom Hearts وزر فتحها في واجهة الهاتف لتظهر مثل بطاقة صور Risen الواضحة.
- [x] مراجعة إزالة منطقة النص أو الشعار في Risen مقابل TIM2، والتحقق من تحديد المنطقة بالسحب والإحداثيات ومن اختيار رقعة خلفية نظيفة.
- [x] تطبيق رقعة الخلفية الأصلية تلقائياً داخل المنطقة المختارة قبل تركيب التعريب، ثم اختبار أن البكسلات خارج المنطقة لا تتغير.
- [x] اختبار بطاقة الهاتف والتحرير الجزئي وبناء ZIP ثم دفع التحديث إلى `main`.

## غلاف بطاقة Kingdom Hearts

- [x] الحصول على غلاف Kingdom Hearts: Birth by Sleep الحقيقي وإضافته بوضوح إلى بطاقة اللعبة في واجهة الهاتف.
- [x] بناء الأداة والتحقق من ظهور الغلاف ثم دفع التحديث إلى `main`.

## سجل الإصدارات

- [x] إضافة التحديثات المنفذة إلى قسم «آخر التحديثات» ورفع رقم الإصدار عند كل دفع إلى `main`.

## تعديل Font.arc الخاص بالباتش الإنجليزي لدعم العربية

- [x] إنشاء نسخة أمان موثقة من `Font.arc` الإنجليزي قبل أي تغيير، وتسجيل حجم الملف وبصمته وبنية موارده.
- [x] تحليل `mesfont.inf` و`mesfont.mtx` لتثبيت الخانات الحرة وخريطة رموز الباتش الإنجليزي دون مساس بالإنجليزية الحالية: عُثر على 140 رمزاً غير مستخدم فعلياً في عينة CTD، وهي تتجاوز 126 شكلاً عربياً مطلوباً.
- [x] تصميم ورسم وربط الحروف العربية وأشكال الاتصال ضمن خانات مؤكدة، مع الحفاظ على معالجة ترتيب العربية الموجودة كما هي.
- [x] تدقيق أبعاد جميع الأشكال العربية قبل الحقن، وبالأخص س/ش/ص/ض/ط/ظ/ف/ق/ك/ل بأشكالها الأربعة، وتضييق أي شكل متجاوز منفرداً من دون تصغير بقية الخط.
- [x] إعادة بناء ملف بديل باسم `Font.arabic.arc` وفحص تطابق كل الموارد غير المستهدفة مع الأصل؛ أضيف منع صريح للرموز المكررة وتحققت مطابقة خريطة الأداة لـ126 رمزاً فريداً.
- [x] إضافة تقرير حقن خط Kingdom Hearts داخل الأداة، وربط تنزيل ملف `Font.arabic.arc` وتقرير الحقن، ثم اختبار البناء وتحديث رقم الإصدار وسجل التحديثات قبل الدفع إلى `main`.

## التدقيق النهائي لخط Kingdom Hearts وربط CTD

- [x] تثبيت نسخ خط الأساس وتدقيق بنية `Font.arabic.arc` مقابل `Font.arc` الأصلي، مع إثبات أن الموارد غير المستهدفة لم تتغير.
- [x] فحص خانات البكسلات الـ126 ورسومها للتأكد من بقائها داخل حد 20×20 ومن عدم وجود خانة مفقودة أو متداخلة؛ كشف الفحص تداخلاً في الخطة الأولى فأعيد البناء بخانات مستقلة.
- [x] مطابقة خريطة `khbbs-arabic-font-map.ts` مع تقرير الحقن وخريطة الخانات، والتحقق من فرادة كل رمز ثنائي البايت.
- [x] اختبار كلمات عربية متصلة عبر مسار بناء CTD، مع تتبع الشكل المنفصل والبداية والوسط والنهاية والرمز المكتوب لكل منها.
- [x] توثيق نتيجة التدقيق، وإصلاح الخلل المثبت فقط، ثم تحديث الإصدار والسجل والدفع إلى `main`.

## إصلاح رفض رمز أثناء بناء CTD في Kingdom Hearts

- [x] اختبار علامة النسبة العربية `٪` (U+066A) كشف أنها تستطيع توليد رسالة مشابهة، لكنها ليست إثباتاً أنها الرمز الموجود في صورة الهاتف.
- [x] عدم إجراء معالجة دائمة لأي رمز قبل أن تظهر قيمته الفعلية في رسالة التشخيص.
- [x] معالجة الرمز المثبت فقط واختبار البناء على النص الفعلي، ثم تحديث الإصدار والسجل والدفع إلى `main`.

## تثبيت الرمز الفعلي من خطأ بناء CTD

- [x] إلغاء الاعتماد على الاستنتاج البصري من الصورة، وتثبيت أن التغييرات المحلية غير المدفوعة لا تضيف شكلاً جديداً أو تطبيعاً لرمز غير مثبت.
- [x] إضافة رسالة تشخيصية تعرض الرمز غير المدعوم ورقم Unicode الخاص به عند رفض بناء CTD؛ اجتاز اختبار Vitest والبناء الكامل.
- [x] ظهرت قيمة الرمز الفعلية من الهاتف: `ר` (U+05E8)، وعولجت بتحويل محدد إلى `r` واختُبر البناء.

## فحص الرموز غير المدعومة داخل محرر Kingdom Hearts

- [x] تحديد نقطة إدماج الفحص في المحرر المشترك وبيانات النصوص الخاصة بملفات CTD.
- [x] إضافة محلل يعد الرموز غير المدعومة، ويعرض الرمز وقيمة Unicode وعدد مرات ظهوره ومواقع النصوص المتأثرة.
- [x] إضافة بطاقة فحص عملية في محرر Kingdom Hearts تتيح الانتقال مباشرة إلى النص المتأثر.
- [x] اختبار الفحص والبناء محلياً، ثم تحديث الإصدار والسجل والدفع إلى `main`.

## الاستبدال التلقائي لرموز Kingdom Hearts

- [x] تعريف تحويلات آمنة لعناصر الهاتف العربية التي لها مقابل إنجليزي في Font.arc، مع استبعاد الحروف العربية نهائياً من التحويل.
- [x] تطبيق التحويل تلقائياً داخل باني CTD، وإظهار الاستبدالات والرموز التي لا تملك مقابلاً آمناً في المحرر.
- [x] اختبار البناء والواجهة ثم تحديث الإصدار والسجل والدفع إلى `main`.

## تبسيط واجهة Kingdom Hearts

- [x] إزالة بطاقة «رموز CTD — Kingdom Hearts» ومكوّنها من المحرر حتى لا تزيد ازدحام الصفحة.
- [x] الإبقاء على الاستبدال التلقائي داخل بناء CTD ورسالة Unicode الواضحة عند وجود رمز لا يمكن تحويله.
- [x] اختبار البناء وتحديث الإصدار والسجل ثم الدفع إلى `main`.

## تنظيف الرموز الدخيلة في بناء Kingdom Hearts

- [x] تحديد سياسة تنظيف آمنة للرموز غير العربية وغير الإنجليزية: تطبيق تحويل محدد فقط للرمز المثبت، لا حذف عام.
- [x] تحويل الرمز العبري الدخيل `ר` (U+05E8) تلقائياً إلى `r` في خرج البناء، مع اختبار يثبت نجاح بناء النص نفسه.
- [x] اختبار البناء وتحديث الإصدار والسجل ثم الدفع إلى `main`.

## تحذير الرموز غير المقبولة داخل محرر Kingdom Hearts

- [x] تحديد مكوّن حقل الترجمة الحالي وربط تحليل الرمز بالنص المفتوح لملفات Kingdom Hearts فقط.
- [x] عرض كل رمز لا يملك مقابلاً آمناً مع قيمة Unicode وعدد تكراره مباشرة تحت حقل الترجمة.
- [x] اختبار التحذير والبناء ثم تحديث الإصدار والسجل والدفع إلى `main`.

## نقل فحص رموز CTD إلى خيارات بناء Kingdom Hearts

- [x] إزالة التحذير من أسفل حقل الترجمة حتى لا يظهر داخل بطاقة النص.
- [x] إضافة زر ضمن خيارات البناء يفعّل فلتر المحرر لعرض النصوص التي تضم رموز CTD غير مدعومة فقط.
- [x] عرض الرمز وUnicode وعدد التكرار في نتائج الفلتر، ثم اختبار الفلتر والبناء والدفع إلى `main`.

## إصلاح إلغاء فلتر رموز CTD في Kingdom Hearts

- [x] تحديد سبب عدم تمكن المستخدم من العودة إلى جميع النصوص بعد تفعيل فلتر الرموز غير المدعومة.
- [x] جعل زر الإلغاء متاحاً دائماً وإضافة مسار واضح لمسح فلتر رموز CTD من واجهة المحرر.
- [x] اختبار التفعيل والإلغاء ثم تحديث الإصدار والسجل والدفع إلى `main`.

## إصلاح تحديد مناطق صور TIM2 في Kingdom Hearts

- [x] فحص سبب تحديد حرف واحد عند اختيار المنطقة تلقائياً أو بالسحب في مورد صورة القوائم.
- [x] جعل التحديد التلقائي والسحب يلتقطان منطقة كاملة قابلة للتعديل، لا بلاطة أو حرفاً منفرداً.
- [x] التحقق من أن الاستبدال يبقي أبعاد TIM2 الأصلية وترميز 8bpp ولوحة الألوان، ثم اختبار البناء والدفع إلى `main`.

## حفظ وتنزيل تحديد صور TIM2 في Kingdom Hearts

- [ ] فحص ما إذا كانت عملية إزالة العنصر تحفظ مستطيل التحديد لاستخدامه في تركيب الصورة التالية.
- [ ] إبقاء التحديد بعد الإزالة واستخدام أبعاده تلقائياً عند تركيب صورة بديلة.
- [ ] إضافة تنزيل المنطقة المحددة كصورة PNG مستقلة، ثم اختبار البناء والدفع إلى `main`.

## تنظيم وتنزيل عدة صور في Kingdom Hearts

- [x] جعل قائمة بطاقات الصور ومرشحات المجلدات قابلة للطي لتوفير مساحة على الهاتف.
- [x] إضافة وضع تحديد متعدد لبطاقات صور TIM2 دون فتح كل مورد.
- [x] تنزيل كل الصور المحددة كملفات PNG دفعة واحدة داخل ZIP، ثم اختبار البناء والدفع إلى `main`.

## ضمان استبدال صور الذكاء الاصطناعي في Kingdom Hearts

- [ ] مراجعة كل مسارات تركيب واستبدال PNG للتحقق من تثبيت عرض وارتفاع TIM2 الأصليين.
- [ ] التحقق من احتفاظ الناتج بترميز 8bpp ولوحة الألوان الأصلية عند استخدام صورة ذات دقة أو ألوان مختلفة.
- [ ] إصلاح أي مسار مخالف، ثم اختبار البناء وتوثيق طريقة الاستبدال للمستخدم قبل الدفع.

## حفظ تعديلات صور Kingdom Hearts داخل الجلسة

- [x] تتبع سبب رجوع مورد TIM2 إلى نسخته الأصلية عند العودة من المحرر إلى القائمة.
- [x] حفظ المورد المعدل في قائمة الصور طوال الجلسة، مع إبقائه ظاهراً ومتاحاً للتنزيل والتجميع.
- [x] اختبار الاستبدال والرجوع وفتح المورد مجدداً، ثم تحديث الإصدار والسجل والدفع إلى `main`.

## استبدال صور Kingdom Hearts في المورد الأصلي

- [x] مراجعة مسار محرر Risen الذي يكتب الصورة المعدلة مباشرة في المورد الأصلي العامل.
- [x] ربط زر تركيب TIM2 في Kingdom Hearts بالمورد الأصلي في مساحة ملفات اللعبة لا بحالة صفحة الصور فقط.
- [ ] اختبار كتابة مورد فعلي من BBS0–BBS4 على جهاز المستخدم، ثم تأكيد استمرار التعديل بعد إغلاق وفتح الملف من جديد.

## فحص باتش Kingdom Hearts Final Mix المرسل

- [x] استخراج وصف الباتش وملفاته ومتطلبات تطبيقه من صفحة المصدر.
- [x] تحديد ما إذا كان الباتش يبدل موارد BBS أو يضيفها، وصلته بمسار النصوص والخطوط والصور في الأداة.
- [x] عرض النتيجة والخطوة المناسبة دون تعديل ملفات اللعبة أو الأداة قبل تأكيد المستخدم.

## إدخال ترجمة Kingdom Hearts في ملفات BBS

- [x] فحص فهرس BBS ومسار إعادة إدراج مورد موجود في `BBS0–BBS3.DAT` مع حماية إزاحات الموارد المجاورة.
- [ ] حفظ نتيجة فحص CTD داخل جلسة BBS ومنع إعادة فحص الموارد عند الرجوع إلى المدير أو المحرر.
- [x] قصر فحص ترويسة CTD على موارد CTD المرشحة وإبقاء فلتر «CTD مؤكد بالترويسة» قابلاً للاستخدام دون نتائج فارغة أثناء الفحص.
- [ ] إعادة فحص ترويسة BBS الكامل عند الحاجة، وعرض CTD المؤكدة فقط لأن مرشحات BBSA قد تضم موارد غير نصية.
- [ ] جعل فلتر امتداد CTD يعرض كل ملفات `.ctd` مباشرة، مع فصل هذا العرض عن فحص الترويسة.
- [ ] إزالة خيار مرشحات CTD من BBSA غير الموثوق، وجعل فحص الترويسة الكامل خطوة مستقلة بعد عرض امتداد CTD.
- [ ] قصر زر فحص الترويسة على نتائج فلتر امتداد `.ctd` المعروضة فقط.
- [ ] إزالة فلتر «مرشحات CTD من فهرس BBSA» من الواجهة وربط زر الفحص بملفات `.ctd` المعروضة فقط.
- [ ] استخراج قائمة ملفات CTD الحقيقية من مصدر أسماء BBS أو جدول CTD.LBA وبناء فلتر امتداد `.ctd` منها.
- [x] إضافة اختيار واحد ظاهر لملف `Font.arabic.arc` في مدير BBS وربطه بالمورد الحقيقي للخط عند بناء DAT.
- [x] تصميم إدخال ملفات CTD المبنية و`Font.arabic.arc` في المورد المطابق من نسخة BBS الإنجليزية الناتجة من XDelta.
- [ ] بناء واجهة قليلة الخطوات تنتج ملفات DAT معدلة جاهزة للإدراج في ISO، من دون طلب تعديل يدوي للملفات الداخلية.
- [ ] اختبار البناء وتحديث الإصدار وسجل التحديثات والدفع إلى `main`.

## ربط محرر Kingdom Hearts بمجلد الباتش

- [ ] فحص مجلدات الباتش الخارجية (`CTD` و`FIX` و`LBA` و`LY2` و`TIM2` و`EXTRA`) وتحديد ما يُحرّر ويُحفظ منها.
- [ ] إضافة فتح مجلد أو ZIP الباتش نفسه، بدلاً من محاولة العثور على هذه المجلدات داخل الروم أو DAT.
- [ ] ربط CTD داخل مجلد الباتش بالمحرر وإخراج ZIP محافظ على المجلدات والملفات غير المعدلة.
- [ ] ربط ملف الخط الموجود في مجلد الباتش الصحيح، ثم اختبار البناء وتحديث الإصدار والدفع إلى `main`.

## تطبيق مواد الباتش على BBS بلا PSP

- [x] الحصول على أرشيف الباتش الفعلي وفهرسة ملفات `CTD` و`FIX` و`LBA` و`LY2` و`TIM2` دون تشغيل ملفاته التنفيذية.
- [x] تحليل كيف يحدد `BBS Patcher.exe` مواضع هذه الموارد ويكتبها في `BBS0–BBS3.DAT`، مع فصل ما يعتمد على DAT مفكوك التشفير.
- [ ] تحديد ما إذا أمكن تنفيذ هذا المسار بأمان داخل المتصفح من الروم الياباني ومواد الباتش، أو توثيق العائق الذي يمنع ذلك.
- [ ] عدم إضافة واجهة «فتح المجلد» وحدها لأنها لا تنتج روم عربي ولا تحل مشكلة المستخدم.

## تدقيق مسار BBS0–BBS3 والخط قبل بناء الملفات النهائية

- [ ] فهرسة برنامج الباتش الأصلي وملفات الجداول للتحقق من كل ملف BBS يقرأه أو يكتبه.
- [ ] إثبات موضع إدراج `Font.arabic.arc` وملفات CTD من منطق الباتش، وعدم افتراض أن BBS0 وحده يكفي.
- [ ] عرض النتيجة المثبتة قبل تعديل أي زر بناء أو إنشاء ملفات نهائية.

## بناء BBS0–BBS3 النهائية من النص والخط العربي

- [x] إصلاح زر البناء فقط ليصدر نسخ BBS0–BBS3 الكاملة، وعدم تغيير وضع الكتابة أو محرر الصور ضمن هذا الإصلاح.
- [x] تثبيت جميع مواضع CTD داخل BBS0 وموضعي `Font.arabic.arc` داخل BBS0 وBBS1 من جداول الباتش الأصلية قبل الكتابة.
- [x] تنفيذ الإدراج على نسخ عمل فقط لملفات CTD والخط التي اختارها المستخدم، مع منع تجاوز المساحة المخصصة ورفض أي ملف لا يطابق الترويسة المتوقعة.
- [x] إنتاج BBS0–BBS3 كاملة فقط، مع بقاء الترويسات والتشفير والحجم وكل CTD أو خط غير مختار وبايتات الموارد الأخرى دون تغيير.
- [x] اختبار البايتات قبل وبعد، ثم إضافة زر بناء نهائي واحد وتحديث الإصدار قبل الدفع.

## إصلاح CTD المتجاوز لسعته داخل BBS0

- [x] قياس المورد `BBS0_D0000000_A927535E.ctd` الذي يحتاج 17,327 بايت مقابل حجزه 14,336 بايت، ومقارنته بالنسخة المقابلة من الباتش الأصلي وجدول CTD.LBA.
- [x] إثبات الطريقة التي يستوعب بها الباتش الأصلي CTD الموسع قبل تعديل باني BBS أو جداول BBSA.
- [x] تنفيذ الإدراج الآمن فقط إذا حُددت مساحة بديلة وجدول لازم تحديثه، مع إثبات أن المورد المجاور لم يتغير.
- [x] اختبار بناء CTD المتجاوز وتحديث الإصدار والسجل ثم الدفع إلى `main`.

## إعادة تخطيط BBS0 عند تجاوز CTD للسعة

- [ ] فحص فهرس BBS0 الحقيقي وأطوال موارده لإثبات حقول الإزاحة والحجم والمحاذاة التي يلزم تحديثها عند نقل مورد.
- [ ] إثبات أن إعادة التخطيط تستطيع نسخ جميع موارد BBS0 كما هي وإعطاء CTD المعدل مساحة أكبر من دون إتلاف الفهرس أو تجاوز حجم الملف.
- [ ] تنفيذ إعادة التخطيط فقط بعد اختبار أرشيف مماثل، والتحقق من تطابق كل مورد غير معدل وفهرسة CTD المنقول.
- [ ] اختبار حالة CTD ذات 17,327 بايت، وتحديث الإصدار والسجل ثم الدفع إلى `main`.

## بناء تجريبي قسري عند تجاوز حجز CTD

- [x] قياس مقدار تجاوز CTD وموضع المورد التالي لإظهار تحذير تجريبي دقيق.
- [x] إضافة مسار بناء قسري يكتب CTD المتجاوز في BBS0 ويُخرج BBS0–BBS3 بناءً على طلب المستخدم.
- [x] اختبار أن البناء القسري لا يغيّر حجم DAT وأن التحذير يسجل المورد المتأثر، ثم تحديث الإصدار والدفع.

## إصلاح فساد مخرجات البناء القسري

- [ ] استلام BBS0 الأصلي الذي أنتج معه البناء القسري قوائم وحواراً برموز غير صحيحة، وفحص سجل الموارد الحقيقي.
- [ ] قياس موضع CTD المتجاوز والمورد الذي أصابه التجاوز، ثم إثبات إعادة ترتيب موارده بلا فقد أو تغيير لبياناته.
- [ ] إزالة الكتابة القسرية من البناء وتنفيذ إعادة تخطيط آمنة قبل إخراج BBS وISO جديدين.
- [ ] تجربة ISO الناتج بعد فحص البايتات وتحديث الإصدار والسجل ثم الدفع إلى `main`.

## تشخيص خط العربية والرموز الغريبة

- [ ] فحص خريطة الرموز العربية وملف `Font.arabic.arc` للتأكد من أن جميع رموز الباني لها بلاطات مرئية وغير فارغة.
- [ ] إثبات موضعي الخط في BBS0 وBBS1 من ملفات المصدر الأصلية ومقارنة المورد بعد إدراج الخط.
- [ ] فصل أثر خط غير صالح عن أثر تجاوز CTD، ثم إزالة البناء القسري من المسار الذي يختبر العربية.
- [ ] اختبار عينة عربية قصيرة داخل قائمة وحوار، وتحديث الإصدار والسجل ثم الدفع إلى `main`.

## إعادة بناء الخط العربي الفارغ

- [ ] توثيق أن معاينة `Font.arabic.arc` الحالية تعرض 126 خلية فارغة، وتحديد خلل التوليد قبل إعادة البناء.
- [ ] إعادة توليد أشكال Noto Naskh Arabic وربطها بخلايا `mesfont.mtx` و`mesfont.inf` مع بكسلات مرئية في المعاينة.
- [ ] فحص كل رمز في خريطة CTD مقابل بلاطة عربية مرئية قبل استبدال الخط داخل BBS0 وBBS1.
- [ ] إزالة البناء القسري واختبار عينة قائمة وحوار ثم تحديث الإصدار والسجل والدفع إلى `main`.

## إدراج BBS0–BBS3 داخل ISO على الهاتف

- [x] فحص بنية ISO-9660 ومسار `PSP_GAME/USRDIR` واختيار قارئ/كاتب محلي يعمل في المتصفح دون رفع ISO.
- [x] إضافة اختيار ISO واستبدال BBS0–BBS3 من مخرجات البناء المفتوحة ثم إخراج ملف ISO جديد.
- [x] رفض الملفات إذا غاب مسار PSP أو اختلف حجم BBS، مع اختبار عدم تغير موارد ISO الأخرى.
- [x] اختبار البناء على ISO صغير مماثل وتحديث الإصدار والسجل ثم الدفع إلى `main`.

## ربط زر بناء CTD الظاهر بمخرج BBS الكامل

- [x] تتبع زر «بناء أرشيف CTD معرّب وتنزيله» الظاهر داخل المحرر وتحديد سبب بقائه على ZIP CTD بعد فتح BBS.
- [x] ربط الزر نفسه ببناء BBS0–BBS3 عندما تكون جلسة ملفات BBS الأربعة مفتوحة، وإيقافه برسالة صريحة بدلاً من تنزيل ZIP CTD في جلسة لا تملك مصادر BBS.
- [x] اختبار البناء وتحديث الإصدار والسجل ثم دفع الإصلاح إلى `main`.

## إصلاح قراءة CTD في Final Mix

- [ ] تصحيح موضع بيانات موارد BBSA في Final Mix: فلتر «CTD مؤكّد بالترويسة» يعرض الآن ٠ ملفاً رغم وجود ٣٦٨ مرشحاً.
- [x] إصلاح واجهة فلتر «CTD مؤكّد بالترويسة» كي لا تعيد قائمة نتائج فارغة (٠ ملف) بسبب فشل تحقق جماعي أو تسمية مؤقتة.
- [ ] اختبار الملفات الفعلية BBS0–BBS4 والتأكد من ظهور CTD الحقيقية، ثم تحديث سجل الإصدار ودفع الإصلاح إلى `main`.

## تشخيص تشوّه نصوص ملفات CTD التابعة للباتش

- [x] فهرسة أرشيف `11.zip` محلياً وإعادة إنتاج ظهور الرموز التقنية عند فتح ملفات CTD، ومقارنة القارئ بالبايتات الخام فقط.
- [x] إثبات مصدر الخلل في ترويسة CTD أو جدول السطور أو فك رموز التحكم قبل إجراء أي تعديل.
- [x] إصلاح القارئ المثبت فقط، وإضافة اختبار بملف CTD مرجعي ثم التحقق من البناء وسجل الإصدار قبل الدفع.

## فحص CTD محدد بقيت فيه نصوص يابانية

- [ ] فحص `BBS0_DB000000_2A78E08A.ctd` من `11.zip` ومقارنة النص الياباني المعروض بالبايتات الخام وتحديد ما إذا كان أصلياً أو خطأ قراءة.
- [ ] عرض الدليل واقتراح إصلاح محدد قبل تغيير قارئ CTD أو واجهة المحرر.

## منع بناء CTD بسبب رموز Shift-JIS الموجودة في النصوص الإنكليزية

- [x] استخراج بايتات Shift-JIS المطابقة للرموز `―` و`∥` و`±` و`×` و`　` و`－` و`｜` من النصوص الأصلية.
- [x] إضافة ترميز عكسي مثبت لهذه الرموز داخل باني CTD، بدلاً من حظرها أو حذفها من نص إنكليزي مترجم.
- [x] إضافة اختبار بناء وحفظ لهذه القائمة كاملة، ثم بناء المشروع وتحديث الإصدار.

## أداء مدير Kingdom Hearts BBS

- [x] إيقاف فحص كل موارد BBS0–BBS4 لحظة الرفع؛ تنفيذ تحقق CTD سريع وعند الطلب فقط كي لا تثقل الأداة أو تتجمد على الهاتف.
- [ ] اختبار الأداء وفلتر CTD، ثم تحديث سجل الإصدار ودفع إصلاح الأداء إلى `main`.

## إزالة قسم Yu-Gi-Oh! Reshef بطلب المستخدم

- [x] حصر جميع بطاقات ومسارات وصفحات ومراجع Yu-Gi-Oh! Reshef فقط.
- [x] حذف مكونات النصوص والصور والجسور والباني الخاصة بـYu-Gi-Oh! Reshef بعد إزالة مراجعها المباشرة.
- [x] بناء الأداة والتحقق من بقاء Pokémon وRisen وبقية المسارات العامة سليمة.
- [x] دفع الالتزام المحلي لإزالة Yu-Gi-Oh! إلى `main` والتحقق من المرجع البعيد.

## إزالة قسمي Fire Emblem بطلب المستخدم

- [x] حصر جميع المسارات والبطاقات والمراجع الخاصة بـFire Emblem DS وFire Emblem: The Sacred Stones فقط.
- [x] حذف ملفات الصفحات والجسور والمكونات التابعة لهذين القسمين بعد إزالة كل مرجع مباشر إليها.
- [x] بناء الأداة والتحقق من بقاء أقسام Pokémon وYu-Gi-Oh! Reshef وRisen والمسارات العامة سليمة.
- [x] دفع الالتزام المحلي لإزالة القسمين إلى `main` والتحقق من المرجع البعيد.

## إصلاح ظهور Fire Emblem: The Sacred Stones

- [x] إضافة بطاقة Sacred Stones إلى صفحة اختيار الألعاب وربطها بالمسار `/fire-emblem-sacred-stones`.
- [x] التحقق بصرياً من ظهور البطاقة في الصفحة الرئيسية ثم دفع التصحيح إلى `main`.

## Fire Emblem: The Sacred Stones — تسميات قائمة البداية

- [ ] تحديد ما إذا كانت تسميات Restart Chapter وCopy Data وErase Data وNew Game وExtras نصوص محرك أو موارد رسومية.
- [ ] استخراج مورد القائمة المثبت وإتاحة مسار تحرير آمن له داخل الأداة فقط بعد توثيق ضغطه ولوحته وخريطة مواضعه.

## تحقيق Fire Emblem 12 — النص المشوّه داخل المحرر

- [ ] مطابقة البايتات المفكوكة من `m/001` و`m/002` بالسجل الذي يدخل المحرر، لإثبات موضع تلفها قبل العرض أو التصدير.
- [ ] قياس عدد سجلات كل ملف `m/` واستبعاد المناطق غير النصية واليابانية من قائمة الترجمة.
- [ ] إصلاح مفكك الضغط أو حدود جدول السجلات فقط، ثم التحقق من سلامة `m/001:0` و`m/002:0` داخل المحرر.

## Fire Emblem 12 — محرر صور القوائم

- [x] تثبيت أن موارد القائمة بنوك بلاطات 4bpp بلا خريطة عرض متاحة، وليست صوراً كاملة قابلة للاستبدال.
- [x] إيقاف الرفع والبناء غير الآمنين، وتحويل الصفحة إلى فاحص تشخيصي يوضح سبب عدم قابلية التحرير.
- [x] بناء التطبيق والتحقق من ظهور رسالة الأمان على صفحة Fire Emblem المحلية.
- [ ] استخراج خريطة مواضع بلاطات شاشة القائمة الحقيقية قبل إعادة تفعيل تنزيل أو رفع أو بناء مورد الصور.
- [ ] مقارنة ملفات NitroFS والبرنامج بين الروم الياباني والروم الإنجليزي Beta 2 لتحديد كل مورد وخريطة مواضع غيّرها الباتش.
- [ ] إثبات ما إذا كانت خريطة NEW GAME وNORMAL وHARD في ملف عنوان مستقل أم ضمن كود ARM9، وتسجيل مسارها وإزاحتها.
- [ ] تحديد مواضع بلاطات NEW GAME وNORMAL وHARD من إطار قائمة البداية الإنجليزية وربط كل موضع برقم بلاطة المورد.
- [ ] إنشاء صورة كاملة منفصلة قابلة للتنزيل لكل كلمة قائمة من خريطة مواضع مثبتة، لا من ترتيب خام متتابع.
- [ ] إعادة توزيع صورة عربية مرفوعة على البلاطات الأصلية فقط، ثم التحقق في ROM مبني عند شاشة القائمة.

- [x] تدقيق عدد سجلات النص المستخرجة وتصنيف الحوار القابل للترجمة والسجلات التقنية غير الآمنة.
- [x] إحصاء فئات السجلات على حدة: أسماء البطاقات، تأثيراتها، الحوارات، واجهة القوائم، والمواقع؛ وتوثيق ما لا يدخل في صيغة النص المباشر.
- [x] مراجعة تغطية glyphs العربية وأشكالها وربطها مع باني Reshef ثنائي الطبقة.
- [x] تحسين المستخرج إذا وُجدت عائلات نصوص إنجليزية صالحة لم تشملها قائمة الـ4,065 سجلًا.
- [x] توثيق أن التعريب الشامل خارج نطاق هذا الإصدار بطلب المستخدم، مع إبقاء حماية الوسوم والبناء جاهزين للترجمة اليدوية لاحقاً.
- [x] حفظ توسيع المستخرج وحماية الوسوم واختباراتها في commit محلي نظيف.
- [x] تحديد مصدر NEW GAME في ROM Reshef وتصنيفه قبل إضافة أي تعديل للمحرر أو للـROM.
- [x] فحص مسار محرر صور Risen واعتماد تدفق محلي مماثل في Yu-Gi-Oh!: ROM ثم معاينة ثم استبدال ثم بناء.
- [x] تعريف المورد الرسومي الموثق NEW GAME وإزاحتَيه وصيغته 4bpp وربطه بالمحرر.
- [x] إضافة محرر صور Yu-Gi-Oh! محلي يعرض المعاينة ويقبل استبدال صورة متوافقة ويحفظ التعديل للجلسة.
- [x] ربط تعديل NEW GAME بباني ROM والتحقق البصري داخل محاكي GBA من أن المورد المعدل هو الرسم الظاهر فعلياً.
- [ ] تتبع موارد CONTINUE وGame Menu وLink Duel وCard Trade وتثبيت إزاحاتها وصيغها قبل إضافتها إلى المحرر.
- [x] إصلاح عرض معاينة محرر الصور على الهاتف وإضافة إرشاد واضح للفصل بين ترجمة النصوص وتعديل الرسومات؛ زر «فتح تعريب النصوص» يصل إلى بطاقة رفع Reshef مباشرة.
- [x] إضافة تنزيل لصورة NEW GAME الأصلية وتطبيق الاستبدال مع حفظ أبعاد 64×16 وخلفية المورد ولوحة ألوانه.
- [x] إضافة محرر Palette محلي وآمن للوحة NEW GAME الموثقة، مع كتابة ألوان هذا المورد فقط إلى ROM عند البناء.
- [x] تتبع شعار Yu-Gi-Oh! Reshef في شاشة العنوان وتثبيت إزاحاته وصيغته واستخدامه للوحة، وإضافة تنزيل الأصل واستبداله الآمن.
- [x] نقل بيانات شعار Reshef المضغوطة إلى مساحة ROM آمنة وتحديث مرجع المورد عندما يتجاوز ضغط LZ77 سعة المورد الأصلية، مع تحقق داخل mGBA.
- [x] تحليل قارئ حوار Reshef المباشر وتخصيص رموز عربية أحادية البايت من دون تعارض مع أوامر المحرك؛ تحقق حي في مشهد Joey أن الرموز تصل إلى renderer كـtokens عربية صحيحة.
- [ ] حصر قارئات النصوص الثانوية، مثل نصوص البطاقات والقوائم، وتعميم hook البايت الواحد عليها بعد التحقق الحي لكل قارئ.
- [ ] تصميم ونشر مخزن نصوص موسع في مساحة ROM آمنة مع إعادة توجيه المؤشرات للنصوص التي تتجاوز سعتها الأصلية.
- [ ] اختبار التوافق داخل المحاكي للنصوص العربية الطويلة والوسوم المحمية قبل إتاحة الباني في الواجهة.
- [x] دفع commits المحلية إلى `main` باستخدام مفتاح GitHub الجديد؛ لا يُحفظ المفتاح داخل المشروع.
- [x] توحيد عداد سعة محرر Reshef وترميز الباني مع جدول العربية أحادي البايت؛ التحقق من سجل الحوار 1526 ومن كل فئة نص قبل إعلان الدعم.
- [ ] توثيق معنى وسوم التحكم #0–#5 من مسار العرض الفعلي، وإظهار شرحها داخل محرر Reshef من دون تغيير بايتاتها في ROM.
- [x] تصنيف سجلات Reshef ذات مؤشرات ROM مثبتة وإتاحة النقل الآمن فقط لها إلى بنك النصوص الموسع عند تجاوز السعة الأصلية.
- [ ] التحقق في mGBA من السجل المنقول الطويل مع وسوم #0 و#1 قبل إتاحة السعة الموسعة للمستخدم.
- [x] ربط الفحص العميق للمشاكل الحرجة بكشف وسوم Reshef #0–#5 و%، وإصلاح الحذف أو اختلاف الترتيب تلقائياً من النص الأصلي.
- [ ] تتبع جدول/مسار الإحالة غير المباشرة لسجل البطاقة ذي سعة 32 بايت والتحقق من إمكان إعادة توجيهه إلى بنك النصوص الموسع داخل mGBA.
- [x] استكمال تحليل مسار سجل البطاقة غير المباشر من ROM المرجعي دون الاعتماد على ملف حفظ خارجي، ثم توثيق ما إذا كان يمكن تحويله بأمان.
- [x] إضافة بطاقة ومسار Fire Emblem: Shin Monshou no Nazo داخل قسم الألعاب في الأداة الأصلية وربطهما بالمحرر العام، من دون إنشاء أداة مستقلة.
- [x] تحليل ROM Fire Emblem DS الأصلي لتحديد بنية NitroFS ومسار النص والخط الأولي والتشغيل الحي قبل تفعيل البناء العربي.
- [x] فحص ROM Fire Emblem: Shin Monshou no Nazo المرسل وتسجيل بصمته وبنية NitroFS من دون تعديل الأصل.
- [x] إعداد مشغل Nintendo DS تشخيصي محلي مع لقطات شاشتي DS والتحكم الآلي وحفظ السجل المرجعي للإقلاع.
- [x] بناء فهرس قابل للبحث لملفات NitroFS والأرشيفات والموارد المضغوطة لتسهيل العثور على النص والخط.
- [x] إعداد أدوات تتبع أولية للـARM9 والـARM7 ومواضع تشغيل الحوار والخط، مع توثيق حد توافق GDB في DeSmuME المحلي.
- [x] تطبيق `FE12_v3_01.delta` ثم `fe12_update_beta2.xdelta` على نسخ مقارنة منفصلة فقط؛ بقيت بصمة ROM الياباني المرجعي مطابقة قبل التطبيق وبعده.
- [x] تحليل فروق باتش الإنجليزية: v3.01 يغير 417 مورداً، منها 91 ملف حوار و`fonts/talk` و`fonts/system`، ويستبدل ARM9 مع بقاء ARM7 كما هو؛ Beta 2 لا يغير الخط أو ARM9/ARM7 ويقتصر عملياً على تصحيحات النصوص والموارد المرتبطة بها.
- [x] توثيق أن رسائل النسخة الإنجليزية الخام تستخدم ASCII بينما أضيفت glyphs الإنجليزية في رموز Shift-JIS كاملة العرض (`0x824F`–`0x8279` و`0x8281`–`0x829A`)، ما يثبت وجود تحويل رموز داخل ARM9 يجب تتبعه قبل تصميم العربية.
- [x] تفكيك صيغة `m/BaseTalk` إلى سجلات حوار وأوامر محرك قابلة للحفظ وإعادة البناء من دون تغيير ROM المصدر.
- [x] مطابقة سجل حوار واحد مستخرج مع المشهد الافتتاحي الحي ومع جدول رموز `fonts/talk`.
- [x] تحديد التصميم الآمن لترميز واتجاه وخط العربية في Fire Emblem DS قبل حقن أي بايت تجريبي.
- [x] إضافة جسر Fire Emblem محلي: قراءة NitroFS، فك/ضغط LZ11، استخراج حوارات `m/`، توليد مورد `fonts/talk` العربي، وإعادة بناء ROM جديد فقط.
- [x] اختبار بنية الجسر على ROM الياباني الأصلي: استخرج أكثر من 1,400 سجل وبُني ROM عربي منفصل مع موارد الحوار والخط.
- [x] اختبار واجهة Fire Emblem على ROM الياباني الأصلي في المتصفح: التحقق الصارم نجح واستُخرجت 3,932 رسالة من 85 ملف حوار ووصلت إلى المحرر المشترك.
- [x] استثناء Fire Emblem من إصلاح الوسوم العام: تسلسل بايتات الحوار المعروض كـ`{XX}` لا يعامل كوسوم قابلة للاستعادة، فلا يمكنه استبدال الترجمة العربية بالمصدر.
- [x] اختبار حفظ الترجمة اليدوية في واجهة Fire Emblem: حُفظت «مرحبا» كما هي في السجل التجريبي ولم تعد بايتات المصدر إلى الحقل؛ تنبيه القِصر هنا متوقع لأن النص اختبار من كلمة واحدة.
- [x] اختبار باني Fire Emblem من واجهة الأداة: أنشأ ROM جديداً من ترجمة «مرحبا»، وضمّن 129 شكلاً عربياً وعدّل موردَي الحوار والخط فقط.
- [x] تحديد سجل حوار Katarina الافتتاحي الظاهر في لقطة المستخدم: `m/201:1`، مع وجود نسختين سينمائيتين كاملتين في `m/016`.
- [x] بناء ROM اختبار مستقل يضع كلمة عربية في سجل `m/201:1` ويحافظ على أوامر الحوار.
- [ ] بدء مسار قصصي جديد والوصول إلى حوار Katarina الافتتاحي للتحقق البصري من الكلمة العربية.
- [ ] استخراج خريطة الفروق الدقيقة بين `fonts/talk` الياباني والإنجليزي، بما يشمل الرموز المستبدلة وأبعاد glyphs والمحاذاة.
- [ ] تتبع تحويل ASCII إلى رموز Shift-JIS كاملة العرض في ARM9 الإنجليزي وتحديد نقطة الربط التي تستخدمها رسائل الحوار.
- [x] مواءمة ترميز العربية وحقن glyphs في جسر الأداة مع آلية الباتش الإنجليزي المثبتة، لا مع افتراض مستقل.
- [ ] بناء ROM اختبار جديد والتحقق البصري من العربية بعد المواءمة، ثم توثيق النتيجة أو إصلاح المتبقي قبل طلب مفتاح GitHub جديد للدفع.
- [ ] تثبيت النسخة الإنجليزية المطبقة محلياً كمرجع للتحليل فقط، وتحديد طريقة قانونية محلية لتمرير باتش الإنجليزية عندما يزوّد المستخدم ملفه الأصلي وملف الباتش.
- [x] حصر خانات `fonts/talk` التي يستخدمها الباتش الإنجليزي فعلاً واستبعادها نهائياً من تخصيص الحروف العربية.
- [x] تعديل واجهة Fire Emblem لتعرض النص الإنجليزي كأساس للترجمة العربية من ROM إنجليزي يرفعه المستخدم، من دون توزيع ROM أو الباتش داخل الأداة.
- [x] اعتماد تطبيق باتشي `FE12_v3_01.delta` ثم `fe12_update_beta2.xdelta` خارج الأداة على الهاتف؛ اختير هذا المسار بدلاً من إعادة تطبيق الباتش في كل جلسة داخل المتصفح.
- [x] مراجعة قسم Risen ومحرر الخط فيه لاستخلاص صيغة تقرير الحقن الذي يعرض المورد والخانات والأكواد والسعة والتحذيرات بعد البناء.
- [x] تعديل Fire Emblem ليقبل ROM الإنجليزي المطبّق عليه الباتش كأساس، ويستخرج حواره الإنجليزي إلى المحرر من دون أي تشغيل للمحاكي أو تتبع قصة.
- [x] إظهار تقرير حقن Fire Emblem بعد البناء: حالة الخط، عدد الأشكال العربية، الخانات المستخدمة والمستبعدة، أكوادها، موارد الحوار المعدلة، والتحذيرات القابلة للتنفيذ.
- [x] اختيار خط TTF عربي مرخّص ومناسب لحجم glyph في Fire Emblem DS، واعتماده مصدراً وحيداً للأشكال العربية بدلاً من مصدر Pokémon GBA.
- [x] إظهار اسم خط TTF وحجم الرستر ومحاذاته ضمن تقرير حقن Fire Emblem حتى يعرف المستخدم مصدر الخط الذي دخل الروم الناتج.
- [x] إنشاء مهارة قابلة للنقل لمحاكي Nintendo DS تشمل تشغيل DeSmuME، إدخال أزرار ولمس الشاشة، التقاط إطار، حدود استخدام ملفات الحفظ، وقالب حالة يتيح المتابعة في محادثة جديدة.
- [x] فحص ملف الروم الذي أرسله المستخدم بعد تطبيق v3.01 ثم Beta 2، وتسجيل بصمته كمدخل Fire Emblem الإنجليزي المعتمد في الأداة.
- [x] تشخيص سبب عدم ظهور قسم Fire Emblem 12 للمستخدم: أضيفت بطاقة قائمة ظاهرة ترتبط بالمسار `/fire-emblem-12`، وتأكد ظهورها محلياً.
- [x] فحص تصدير Fire Emblem الإنجليزي وإصلاح تشوه الحوار: كان مفكك LZ11 يقدّم مصدر النسخ المتداخل بايتاً واحداً، فصار النص الإنجليزي واضحاً دون استبعاد سجلات حوار صحيحة.
- [x] تحديد موارد واجهة البداية الإنجليزية: وصف بدء اللعبة ودرجات الصعوبة نصوص في `m/MM`، أما NEW GAME وNORMAL وHARD فهي رسومات 4bpp موثقة في `title/mainsave.cg` و`title/modeselect.cg`، وقد أضيف لها محرر صور وبناء LZ10 محلي مختبر.
- [ ] مقارنة كل ملفات NitroFS بين ROM الياباني المرجعي وROM الإنجليزي Beta 2، واستخراج جميع النصوص الإنجليزية التي غيّرها الباتش وربط النصوص أو الرسومات القابلة للتحرير بالمحرر المناسب.

## Fire Emblem: The Sacred Stones — فحص أولي للقراءة فقط

- [ ] فك حزمة المستخدم وتحديد ملف ROM الإنجليزي وبصمته، من دون تعديل أي ملف داخل الحزمة.
- [ ] تشغيل ROM في المحاكي والوصول إلى أول نص ظاهر بلقطة مرجعية واحدة فقط، ثم مطابقة شكل خطه مع مورد الخط المحتمل؛ لا تعديل ولا متابعة للقصة.
- [ ] فحص عينات النص الإنجليزي ومؤشرات الحوار لتحديد صيغة النص وسلامة الاستخراج.
- [ ] تحديد مورد الخط وبنيته وخاناته المحتملة قبل اقتراح حقن عربي.
- [ ] التحقق هل نصوص القوائم نص مباشر أم رسوم، ثم تقديم قرار عملي واضح قبل إنشاء قسم جديد في الأداة.
